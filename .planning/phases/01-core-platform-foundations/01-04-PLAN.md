---
phase: 01-core-platform-foundations
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/modules/directory/application/role-service.ts
  - src/modules/directory/application/user-service.ts
  - src/modules/directory/adapters/memory/in-memory-tenant-repository.ts
  - src/modules/directory/adapters/memory/in-memory-role-repository.ts
  - src/modules/directory/adapters/memory/in-memory-user-repository.ts
  - src/modules/directory/adapters/convex/mappers.ts
  - src/modules/directory/adapters/convex/convex-tenant-repository.ts
  - src/modules/directory/adapters/convex/convex-role-repository.ts
  - src/modules/directory/adapters/convex/convex-user-repository.ts
  - src/modules/directory/index.ts
  - convex/directory.ts
  - tests/_helpers/tenant-context-fixture.ts
  - tests/_helpers/in-memory-fakes.ts
  - tests/modules/directory/role-service.test.ts
  - tests/modules/directory/user-service.test.ts
  - tests/modules/directory/tenant-isolation.test.ts
  - tests/modules/directory/cycle-prevention.test.ts
  - tests/modules/directory/convex-role-repository.test.ts
  - tests/modules/directory/convex-user-repository.test.ts
autonomous: false
requirements:
  - CON-01
  - CON-02
  - CON-03
  - CON-04
tags:
  - services
  - adapters
  - vitest
  - tenant-isolation
  - cycle-prevention
  - convex-push
  - blocking

user_setup:
  - service: convex
    why: "First-time `npx convex dev` provisions a Convex development deployment interactively (deployment name, project selection). Subsequent runs are non-interactive when CONVEX_DEPLOY_KEY is set."
    env_vars:
      - name: CONVEX_DEPLOYMENT
        source: "Set automatically by `npx convex dev` first-run; written into .env.local"
      - name: CONVEX_DEPLOY_KEY
        source: "Convex Dashboard -> Project Settings -> Deploy Keys (required for CI / non-interactive deploy)"
    dashboard_config:
      - task: "Confirm dev deployment is provisioned and `convex/schema.ts` is pushed (six tables visible in Convex Dashboard Data tab)"
        location: "Convex Dashboard -> Data"

must_haves:
  truths:
    - "RoleService implements all six D-17 methods (createRole, findRoleById, findRoleByName, listRolesByTenant, renameRole, deleteRole), each taking TenantContext as first parameter"
    - "UserService implements all eight D-17 methods (createUser, findUserById, findUserByEmail, listUsersByTenant, updateUserProfile, assignRole, setManager, deleteUser), each taking TenantContext as first parameter"
    - "UserService.setManager enforces cycle prevention per RESEARCH.md Pattern 7 — rejects direct self-loop, 2-cycle, N-cycle, depth > 50; allows broken chains and null assignment"
    - "UserService.assignRole verifies the role belongs to the same TenantContext (T-1-RP-01 mitigation — role assignment privilege escalation)"
    - "In-memory repository fakes implement all three ports (Tenant, Role, User) with Map-backed storage that respects tenant scoping (cross-tenant returns null)"
    - "Convex adapter mappers translate Convex Id<table> to/from branded domain IDs at every boundary"
    - "Convex repositories use .withIndex('by_tenant_*', q => q.eq('tenantId', ...)) on every query (D-09 enforcement at adapter layer)"
    - "convex/directory.ts wires DI per RESEARCH.md Pattern 9: input validation -> resolve TenantContext -> instantiate repo + service -> call method -> map response — services and non-ids domain types imported through @/modules/directory barrel (Zone 7); adapter classes deep-imported from src/modules/directory/adapters/convex/** (allowed by Zone 7 since adapters are not barrel-exposed)"
    - "Vitest suite achieves 90%+ coverage on tenant isolation, role uniqueness, manager assignment + cycle prevention, role lookup, user lookup (D-20 + engineering.md)"
    - "Convex schema is PUSHED to the live deployment via `npx convex dev` (interactive first-run) — BLOCKING checkpoint task"
    - "After schema push, `npx gitnexus analyze` rebuilds the code index (CLAUDE.md requirement after first code lands)"
  artifacts:
    - path: "src/modules/directory/application/role-service.ts"
      provides: "RoleService class with all six D-17 methods; throws RoleNameAlreadyExistsError and RoleNotFoundError appropriately"
      contains: "class RoleService"
    - path: "src/modules/directory/application/user-service.ts"
      provides: "UserService class with all eight D-17 methods; setManager implements cycle prevention; assignRole verifies cross-tenant role assignment is rejected"
      contains: "class UserService"
    - path: "src/modules/directory/adapters/memory/in-memory-role-repository.ts"
      provides: "InMemoryRoleRepository implementing RoleRepositoryPort; Map-backed; enforces tenant isolation in findById/findByName"
      contains: "InMemoryRoleRepository"
    - path: "src/modules/directory/adapters/memory/in-memory-user-repository.ts"
      provides: "InMemoryUserRepository implementing UserRepositoryPort; Map-backed; enforces tenant isolation"
      contains: "InMemoryUserRepository"
    - path: "src/modules/directory/adapters/memory/in-memory-tenant-repository.ts"
      provides: "InMemoryTenantRepository implementing TenantRepositoryPort"
      contains: "InMemoryTenantRepository"
    - path: "src/modules/directory/adapters/convex/mappers.ts"
      provides: "Pure mapper functions toTenantId/fromTenantId/toUserId/fromUserId/toRoleId/fromRoleId; document-to-entity mappers for Tenant, User, Role"
      contains: "toTenantId"
    - path: "src/modules/directory/adapters/convex/convex-role-repository.ts"
      provides: "ConvexRoleRepository implementing RoleRepositoryPort against MutationCtx; uses by_tenant_name index for all reads"
      contains: "ConvexRoleRepository"
    - path: "src/modules/directory/adapters/convex/convex-user-repository.ts"
      provides: "ConvexUserRepository implementing UserRepositoryPort; uses by_tenant_email + by_tenant_role + by_tenant_manager indexes"
      contains: "ConvexUserRepository"
    - path: "convex/directory.ts"
      provides: "Convex mutations and queries wired via DI (Pattern 9): createRole, findRoleById, findRoleByName, listRolesByTenant, renameRole, deleteRole, createUser, findUserById, findUserByEmail, listUsersByTenant, updateUserProfile, assignRole, setManager, deleteUser"
      contains: "RoleService"
    - path: "tests/modules/directory/role-service.test.ts"
      provides: "Vitest tests for all RoleService methods including duplicate-name rejection and cross-tenant isolation"
    - path: "tests/modules/directory/user-service.test.ts"
      provides: "Vitest tests for all UserService methods including assignRole cross-tenant rejection"
    - path: "tests/modules/directory/tenant-isolation.test.ts"
      provides: "Vitest tests verifying TenantContext A cannot read TenantContext B data via any service method"
    - path: "tests/modules/directory/cycle-prevention.test.ts"
      provides: "Vitest tests for cycle prevention: direct self-loop, 2-cycle, N-cycle, broken chain (allowed), depth-limit (51)"
    - path: "tests/modules/directory/convex-role-repository.test.ts"
      provides: "Adapter mapping test for ConvexRoleRepository: Id<'roles'> to/from RoleId round-trip + document-to-entity shape"
    - path: "tests/modules/directory/convex-user-repository.test.ts"
      provides: "Adapter mapping test for ConvexUserRepository"
    - path: "tests/_helpers/tenant-context-fixture.ts"
      provides: "Filled-in TenantContext factory for tests (replaces the placeholder from plan 01-01)"
    - path: "tests/_helpers/in-memory-fakes.ts"
      provides: "Helper to spin up a fresh InMemoryRoleRepository + InMemoryUserRepository + RoleService + UserService for each test"
  key_links:
    - from: "src/modules/directory/application/role-service.ts"
      to: "src/modules/directory/ports/role-repository.port.ts"
      via: "constructor-injected port"
      pattern: "RoleRepositoryPort"
    - from: "src/modules/directory/application/user-service.ts"
      to: "src/modules/directory/ports/user-repository.port.ts and role-repository.port.ts"
      via: "constructor-injected ports"
      pattern: "UserRepositoryPort.*RoleRepositoryPort"
    - from: "src/modules/directory/adapters/convex/convex-role-repository.ts"
      to: "convex/schema.ts by_tenant_name index"
      via: ".withIndex('by_tenant_name', ...)"
      pattern: "by_tenant_name"
    - from: "src/modules/directory/adapters/convex/convex-user-repository.ts"
      to: "convex/schema.ts by_tenant_email + by_tenant_manager indexes"
      via: ".withIndex"
      pattern: "by_tenant_email"
    - from: "convex/directory.ts createRole mutation"
      to: "src/modules/directory/application/role-service.ts RoleService.createRole"
      via: "Pattern 9 DI wiring"
      pattern: "RoleService"
---

<objective>
Implement the application layer (RoleService, UserService) per D-17/D-18/D-19, the Convex persistence adapters per RESEARCH.md Patterns 4/8, the in-memory repository fakes for service tests per Pattern 6, the Convex mutation/query handlers per Pattern 9, the full Vitest test suite per D-20 with 90%+ coverage on critical paths, and run the BLOCKING Convex schema push to the live deployment so subsequent phases can build against real persistence.

Purpose: This plan delivers all the behavior Phase 1 promises — tenant isolation, role uniqueness, manager cycle prevention, stable user-role references through roleId — verified by 90%+ test coverage. It also resolves the schema push BLOCKING requirement (per the schema_push_requirement in the planning context) — without `npx convex dev` running once to provision the deployment and push convex/schema.ts, the Phase 1 verification state is a false positive (TypeScript types come from convex/\_generated codegen, not the live database).

Output: Working `npm test` with 90%+ coverage on critical paths; convex/directory.ts wired with all 14 mutations + queries; Convex deployment provisioned and schema pushed; `npx gitnexus analyze` re-run to refresh the code intelligence index.
</objective>

<execution_context>
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-platform-foundations/01-CONTEXT.md
@.planning/phases/01-core-platform-foundations/01-RESEARCH.md
@.planning/phases/01-core-platform-foundations/01-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@ARCHITECTURE.md
@src/modules/directory/index.ts
@src/modules/directory/domain/ids.ts
@src/modules/directory/domain/user.ts
@src/modules/directory/domain/role.ts
@src/modules/directory/domain/tenant.ts
@src/modules/directory/application/tenant-context.ts
@src/modules/directory/application/errors.ts
@src/modules/directory/ports/role-repository.port.ts
@src/modules/directory/ports/user-repository.port.ts
@src/modules/directory/ports/tenant-repository.port.ts
@convex/schema.ts
@convex/directory.ts
@convex/README.md
@CLAUDE.md

<interfaces>
Contracts this plan implements against (already defined by plans 01-02 and 01-03):

From src/modules/directory (barrel):

```typescript
export interface TenantContext {
  readonly tenantId: TenantId;
}
export const tenantContext: (tenantId: TenantId) => TenantContext;
export interface RoleRepositoryPort {
  create(ctx: TenantContext, input: { name: string }): Promise<Role>;
  findById(ctx: TenantContext, id: RoleId): Promise<Role | null>;
  findByName(ctx: TenantContext, name: string): Promise<Role | null>;
  listByTenant(ctx: TenantContext): Promise<Role[]>;
  rename(ctx: TenantContext, id: RoleId, newName: string): Promise<Role>;
  delete(ctx: TenantContext, id: RoleId): Promise<void>;
}
export interface UserRepositoryPort {
  /* 8 methods, TenantContext-first */
}
export interface TenantRepositoryPort {
  create(input): Promise<Tenant>;
  findById(id): Promise<Tenant | null>;
}
```

From convex/\_generated/dataModel.d.ts (produced by plan 01-03 codegen):

```typescript
export type Id<T extends TableNames> = string & { __tableName: T };
export type Doc<T extends TableNames> = { _id: Id<T>; _creationTime: number; ...schema fields... };
```

D-17 service surface (already declared in CONTEXT.md):

- RoleService: createRole, findRoleById, findRoleByName, listRolesByTenant, renameRole, deleteRole
- UserService: createUser, findUserById, findUserByEmail, listUsersByTenant, updateUserProfile, assignRole, setManager, deleteUser
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RoleService + UserService + in-memory fakes + test helpers + full Vitest suite (90%+ coverage)</name>
  <files>
    src/modules/directory/application/role-service.ts
    src/modules/directory/application/user-service.ts
    src/modules/directory/adapters/memory/in-memory-tenant-repository.ts
    src/modules/directory/adapters/memory/in-memory-role-repository.ts
    src/modules/directory/adapters/memory/in-memory-user-repository.ts
    src/modules/directory/index.ts
    tests/_helpers/tenant-context-fixture.ts
    tests/_helpers/in-memory-fakes.ts
    tests/modules/directory/role-service.test.ts
    tests/modules/directory/user-service.test.ts
    tests/modules/directory/tenant-isolation.test.ts
    tests/modules/directory/cycle-prevention.test.ts
  </files>
  <behavior>
    - RoleService.createRole rejects duplicate name within same tenant (throws RoleNameAlreadyExistsError); allows same name in different tenants
    - RoleService.findRoleByName returns null when name belongs to a different tenant (tenant isolation)
    - RoleService.findRoleById returns null when role belongs to a different tenant
    - RoleService.renameRole rejects rename when new name already exists in same tenant; allows rename when new name is free
    - UserService.createUser persists the user with the given roleId; rejects creation when roleId belongs to a different tenant (T-1-RP-01 privilege escalation mitigation)
    - UserService.findUserByEmail returns null when email belongs to a user in a different tenant (tenant isolation through index)
    - UserService.assignRole verifies the new roleId belongs to the same TenantContext; rejects cross-tenant role assignment with RoleNotFoundError (privilege escalation guard)
    - UserService.assignRole, after a role is renamed, still resolves to the same role via roleId — proves CON-03 stable-reference behavior
    - UserService.setManager rejects direct self-loop (A.setManager(A) throws ManagerCycleError)
    - UserService.setManager rejects 2-cycle (A->B then B->A throws)
    - UserService.setManager rejects N-cycle (A->B->C then A.manager=C throws)
    - UserService.setManager allows broken chain (manager-of-manager missing — does not block assignment)
    - UserService.setManager triggers depth-limit error when chain exceeds 50 (synthetic chain of 51)
    - UserService.setManager(userId, null) clears manager successfully
    - UserService.setManager rejects manager from different tenant (ManagerNotFoundError because repo returns null for cross-tenant)
    - All InMemory repositories enforce tenant isolation: findById/findByName/findByEmail return null when the requested entity belongs to a different tenant
  </behavior>
  <read_first>
    - .planning/phases/01-core-platform-foundations/01-CONTEXT.md (D-17 service surface, D-18 constructor-injected ports, D-19 TenantContext-first methods, D-20 test priorities + 90% coverage on critical paths)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md Pattern 5 "Constructor-Injected Service with TenantContext" (RoleService skeleton — extend with all six D-17 methods)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md Pattern 6 "In-Memory Repository Fake for Service Tests" (InMemoryRoleRepository template — mirror for User and Tenant)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md Pattern 7 "Cycle Prevention in UserService.setManager" (full algorithm including MAX_MANAGER_CHAIN_DEPTH = 50, all five test cases)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md Pattern 10 "Vitest Service Tests" (test file structure template)
    - .planning/phases/01-core-platform-foundations/01-VALIDATION.md "Phase Requirements -> Test Map" (one row per behavior; test file naming convention)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md "Common Pitfalls" Pitfall 5 (exactOptionalPropertyTypes — managerId is `UserId | null` always present, never optional)
    - src/modules/directory/application/errors.ts (the five error classes already defined by plan 01-02)
    - src/modules/directory/ports/role-repository.port.ts, user-repository.port.ts, tenant-repository.port.ts (the contracts services implement against)
    - src/modules/directory/application/tenant-context.ts (TenantContext type + factory)
    - src/modules/directory/index.ts (current barrel — services need to be added)
    - tests/_helpers/tenant-context-fixture.ts (currently a placeholder from plan 01-01 — replace with real implementation)
    - vitest.config.ts (coverage thresholds — 90% on all four metrics)
  </read_first>
  <action>
    Implement RoleService per RESEARCH.md Pattern 5 expanded to all six D-17 methods:
    - createRole(ctx, {name}): call repo.findByName(ctx, name); throw RoleNameAlreadyExistsError if exists; else repo.create
    - findRoleById(ctx, id): delegate to repo.findById
    - findRoleByName(ctx, name): delegate to repo.findByName
    - listRolesByTenant(ctx): delegate to repo.listByTenant
    - renameRole(ctx, id, newName): repo.findById to confirm role exists; throw RoleNotFoundError if null; repo.findByName(newName) to check no collision; throw RoleNameAlreadyExistsError if exists; else repo.rename
    - deleteRole(ctx, id): repo.findById to confirm; throw RoleNotFoundError if null; else repo.delete

    Implement UserService per D-17 (eight methods). Constructor takes `userRepo: UserRepositoryPort` and `roleRepo: RoleRepositoryPort`:
    - createUser(ctx, {email, name, roleId, managerId}): repo.findByEmail to check uniqueness; throw new EmailAlreadyExistsError(input.email) if findByEmail returns an existing user (EmailAlreadyExistsError is exported from src/modules/directory by plan 01-02 — no conditional handling needed); verify roleId belongs to same tenant via roleRepo.findById (throw RoleNotFoundError if null — T-1-RP-01 privilege escalation guard); if managerId !== null, verify manager exists in same tenant via userRepo.findById (throw ManagerNotFoundError if null); else create
    - findUserById(ctx, id): delegate
    - findUserByEmail(ctx, email): delegate
    - listUsersByTenant(ctx): delegate
    - updateUserProfile(ctx, id, {name}): findById to verify; throw UserNotFoundError if null; delegate
    - assignRole(ctx, userId, newRoleId): findById to verify user exists in tenant (throw UserNotFoundError); roleRepo.findById to verify newRoleId belongs to same tenant (throw RoleNotFoundError — privilege escalation guard); delegate to userRepo.updateRole
    - setManager(ctx, userId, newManagerId): implement RESEARCH.md Pattern 7 verbatim. Constant MAX_MANAGER_CHAIN_DEPTH = 50. If newManagerId is null, delegate to updateManagerId(null). If newManagerId === userId, throw ManagerCycleError. userRepo.findById(newManagerId) to verify manager exists in tenant (throw ManagerNotFoundError if null). Walk upward from newManager.managerId; if cursor === userId at any step, throw ManagerCycleError; if steps > MAX_MANAGER_CHAIN_DEPTH, throw a generic Error citing data corruption. If chain breaks (findById returns null mid-walk), accept the assignment. Finally delegate updateManagerId
    - deleteUser(ctx, id): findById to verify; throw UserNotFoundError if null; delegate

    Implement InMemoryRoleRepository per RESEARCH.md Pattern 6. Map-backed (Map<string, Role> keyed by id). Counter for generated IDs. Every method takes ctx, every read filters role.tenantId === ctx.tenantId (return null for cross-tenant) — this is the in-memory mirror of the tenant-prefixed index pattern.

    Implement InMemoryUserRepository similarly. Map-backed. Implement all 8 port methods. For findByEmail: scan all users, return first match where email === query AND tenantId === ctx.tenantId. For updateRole and updateManagerId: read-modify-write the Map entry; preserve other fields.

    Implement InMemoryTenantRepository (used only by tenant bootstrap; no tenant-scoped methods).

    Update src/modules/directory/index.ts (the barrel): add `export { RoleService } from "./application/role-service.js"` and `export { UserService } from "./application/user-service.js"`. The six error classes (including EmailAlreadyExistsError) are already exported by plan 01-02 — no error-export changes needed here. DO NOT export the in-memory repositories from the barrel — they are test infrastructure, not public API (downstream test files import them via the barrel only because test code can import from anywhere; production code consumes ports, not concrete adapters).

    Replace placeholder tests/_helpers/tenant-context-fixture.ts:
    ```typescript
    import { tenantContext, tenantId, type TenantContext, type TenantId } from "@/modules/directory";
    export const TENANT_A: TenantContext = tenantContext(tenantId("tenant_a"));
    export const TENANT_B: TenantContext = tenantContext(tenantId("tenant_b"));
    export const makeTenantContext = (raw: string): TenantContext => tenantContext(tenantId(raw));
    ```

    Create tests/_helpers/in-memory-fakes.ts — exports a `setupDirectory()` helper that constructs fresh InMemoryTenantRepository + InMemoryRoleRepository + InMemoryUserRepository, instantiates RoleService and UserService, returns { tenantRepo, roleRepo, userRepo, roleService, userService }. Use this in every test's beforeEach.

    Write the four test files per RESEARCH.md Pattern 10 + Pattern 7 test enumeration:

    tests/modules/directory/role-service.test.ts:
    - describe blocks: createRole, findRoleById, findRoleByName, listRolesByTenant, renameRole, deleteRole
    - At minimum the tests listed in the <behavior> block above for RoleService

    tests/modules/directory/user-service.test.ts:
    - describe blocks: createUser, findUserByEmail, updateUserProfile, assignRole, deleteUser
    - Tests for createUser cross-tenant roleId rejection, assignRole stable-reference across rename, profile update preserves other fields

    tests/modules/directory/tenant-isolation.test.ts:
    - For each TenantContext-aware method on both services, verify that data created in TENANT_A is invisible from TENANT_B
    - At least: findRoleById, findRoleByName, listRolesByTenant, findUserById, findUserByEmail, listUsersByTenant
    - For mutations across tenants: assignRole, setManager cross-tenant attempts throw

    tests/modules/directory/cycle-prevention.test.ts:
    - Direct self-loop (A.setManager(A) throws ManagerCycleError)
    - 2-cycle (A.manager=B success, then B.manager=A throws)
    - N-cycle (A->B->C chain, then A.manager=C throws)
    - Broken chain (B.manager=C, C is deleted from repo manually via the InMemory map for the test, then A.manager=B succeeds — proves Pattern 7 broken-chain branch)
    - Depth-limit trigger: build a chain of 51 users (u0 manages u1, u1 manages u2, ..., u49 manages u50). Setting newUser.manager = u0 must throw the depth-limit Error
    - setManager(userId, null) successfully clears manager
    - setManager with newManagerId from a different tenant throws ManagerNotFoundError

    Run `npx vitest run --coverage` to verify coverage thresholds (90% on lines/branches/functions/statements per vitest.config.ts). If coverage falls below threshold, add tests for the uncovered branches before completing.

  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx eslint src/modules/directory tests/modules/directory tests/_helpers --max-warnings 0 &amp;&amp; npx vitest run tests/modules/directory --reporter=dot &amp;&amp; npx vitest run --coverage --reporter=dot</automated>
  </verify>
  <done>
    - RoleService and UserService classes implement every D-17 method, each taking TenantContext as first param (D-19)
    - UserService.setManager implements Pattern 7 cycle prevention with MAX_MANAGER_CHAIN_DEPTH = 50
    - UserService.assignRole and createUser verify roleId belongs to same tenant before persisting (T-1-RP-01 mitigation)
    - InMemoryRoleRepository, InMemoryUserRepository, InMemoryTenantRepository implement their ports; in-memory tenant isolation mirrors the production index pattern
    - All five behaviors listed in <behavior> have corresponding test cases
    - `npx vitest run --coverage` exits 0 AND coverage thresholds (90% on all four metrics) are met for src/modules/directory/application and src/modules/directory/adapters/memory
    - Barrel updated to export RoleService and UserService; in-memory repositories are NOT in the barrel (test code imports directly)
    - tests/_helpers/tenant-context-fixture.ts now exports TENANT_A, TENANT_B, makeTenantContext (replacing plan 01-01 placeholder)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Convex adapters (mappers + repositories) + wire convex/directory.ts handlers (Pattern 9) + adapter mapping tests</name>
  <files>
    src/modules/directory/adapters/convex/mappers.ts
    src/modules/directory/adapters/convex/convex-tenant-repository.ts
    src/modules/directory/adapters/convex/convex-role-repository.ts
    src/modules/directory/adapters/convex/convex-user-repository.ts
    convex/directory.ts
    tests/modules/directory/convex-role-repository.test.ts
    tests/modules/directory/convex-user-repository.test.ts
  </files>
  <behavior>
    - toTenantId/fromTenantId, toUserId/fromUserId, toRoleId/fromRoleId are pure pass-through casts: round-trip via toX(fromX(brandedId)) === brandedId for every branded ID
    - roleDocToEntity maps a Convex Doc<'roles'> shape to a Role domain entity with branded IDs
    - userDocToEntity maps a Convex Doc<'users'> shape to a User domain entity; preserves null vs undefined per Pitfall 5
    - tenantDocToEntity maps a Convex Doc<'tenants'> shape to a Tenant domain entity
    - ConvexRoleRepository.findByName uses .withIndex("by_tenant_name", q => q.eq("tenantId", fromTenantId(ctx.tenantId)).eq("name", name)).unique() — verified in adapter test via mock MutationCtx
    - ConvexRoleRepository.listByTenant uses .withIndex("by_tenant_name", q => q.eq("tenantId", fromTenantId(ctx.tenantId))).collect() — prefix-only query
    - ConvexUserRepository.findByEmail uses by_tenant_email prefix
    - ConvexUserRepository.create writes user record with managerId: null (explicit) when input.managerId is null — Pitfall 5
    - convex/directory.ts createRole mutation accepts args { tenantId: v.id("tenants"), name: v.string() }, constructs TenantContext via mappers, instantiates ConvexRoleRepository + RoleService, calls service, returns the Role with plain-string IDs
    - Every convex/directory.ts handler follows Pattern 9: validate input -> resolve TenantContext -> DI -> call service -> map response
  </behavior>
  <read_first>
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md Pattern 4 "Convex Id <-> Branded Domain ID Mapping at Adapter Boundary" (full mappers.ts template — use VERBATIM as starting point; extend with userDocToEntity and tenantDocToEntity)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md Pattern 8 "Convex Adapter — Read-Then-Write Uniqueness Check" (ConvexRoleRepository template — extend with all six port methods)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md Pattern 9 "Convex Mutation Handler — DI Assembly Point" (full createRole mutation example — replicate the structure for all 14 D-17 methods)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md "Common Pitfalls" Pitfall 1 (Convex v.id vs branded ID — every incoming args.tenantId must go through toTenantId mapper)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md "Common Pitfalls" Pitfall 2 (Convex OCC — application-layer uniqueness check is correct without distributed locks)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md "Common Pitfalls" Pitfall 6 (per-mutation DI; no module-level service singletons)
    - .planning/phases/01-core-platform-foundations/01-CONTEXT.md (convex/ HARD RULE — convex/directory.ts is DI wiring only)
    - convex/schema.ts (table shapes — adapters must read fields per the schema definition; index names must match exactly)
    - convex/_generated/dataModel.d.ts (Id<T> and Doc<T> types)
    - convex/directory.ts (currently skeleton from plan 01-03; this task fills it in)
    - src/modules/directory/application/role-service.ts and user-service.ts (just written by Task 1 — services that convex/directory.ts will instantiate)
    - src/modules/directory/index.ts (barrel — convex/directory.ts imports RoleService, UserService, TenantContext, tenantContext, error types through barrel)
    - eslint.config.js (Zone 6: convex/ may not deep-import domain entities except domain/ids.ts; Zone 7: convex/ must import application services + non-ids domain types through @/modules/directory barrel — but adapters/convex/** deep imports remain allowed for DI wiring)
  </read_first>
  <action>
    Create src/modules/directory/adapters/convex/mappers.ts per RESEARCH.md Pattern 4 extended. Include:
    - Six pure ID mappers: toTenantId, fromTenantId, toUserId, fromUserId, toRoleId, fromRoleId
    - Three document-to-entity mappers: tenantDocToEntity, roleDocToEntity, userDocToEntity. Each takes a Doc<table> from convex/_generated/dataModel and returns the branded domain entity. For userDocToEntity, the input doc has optional fields (name, image, emailVerificationTime, isAnonymous, phone) per the schema — the mapper normalizes undefined -> null per Pitfall 5 to keep the domain type's `| null` encoding consistent. managerId is already v.union(v.id, v.null) at schema, so passes through.

    Create src/modules/directory/adapters/convex/convex-role-repository.ts per RESEARCH.md Pattern 8 extended to all six port methods:
    - constructor(private readonly db: MutationCtx["db"]) — accepts the ctx.db handle (Pattern 9; the convex/directory.ts handler passes ctx.db when instantiating)
    - create(ctx, {name}): db.insert("roles", { tenantId: fromTenantId(ctx.tenantId), name, createdAt: Date.now() }); db.get to fetch the inserted doc; return via roleDocToEntity
    - findById(ctx, id): db.get(fromRoleId(id)); return null if not found OR if doc.tenantId !== fromTenantId(ctx.tenantId) (tenant isolation belt-and-braces); else roleDocToEntity
    - findByName(ctx, name): per Pattern 8 — db.query("roles").withIndex("by_tenant_name", q => q.eq("tenantId", fromTenantId(ctx.tenantId)).eq("name", name)).unique(); return null or roleDocToEntity
    - listByTenant(ctx): db.query("roles").withIndex("by_tenant_name", q => q.eq("tenantId", fromTenantId(ctx.tenantId))).collect(); map all via roleDocToEntity
    - rename(ctx, id, newName): db.patch(fromRoleId(id), { name: newName }); db.get; return roleDocToEntity
    - delete(ctx, id): db.delete(fromRoleId(id))

    Create src/modules/directory/adapters/convex/convex-user-repository.ts similarly using by_tenant_email + by_tenant_role + by_tenant_manager indexes:
    - findByEmail: .withIndex("by_tenant_email", q => q.eq("tenantId", ...).eq("email", email)).unique()
    - listByTenant: .withIndex("by_tenant_email", q => q.eq("tenantId", ...)).collect() (prefix query)
    - create takes the full input (email, name, roleId, managerId) — write all fields, defaulting name/image/etc. to null/undefined per schema's v.optional
    - updateRole, updateManagerId, updateProfile use db.patch
    - All eight port methods implemented

    Create src/modules/directory/adapters/convex/convex-tenant-repository.ts:
    - create({name}): db.insert("tenants", {name, createdAt: Date.now()}); db.get; return tenantDocToEntity
    - findById(id): db.get(fromTenantId(id)); return null or tenantDocToEntity

    Rewrite convex/directory.ts per RESEARCH.md Pattern 9. Define 14 named exports — one mutation or query for each D-17 method:
      Roles (mutations: createRole, renameRole, deleteRole; queries: findRoleById, findRoleByName, listRolesByTenant)
      Users (mutations: createUser, updateUserProfile, assignRole, setManager, deleteUser; queries: findUserById, findUserByEmail, listUsersByTenant)

    Every handler follows the SAME Pattern 9 shape:
      1. Convex args validator covers exact shape (use v.id, v.string, v.union(v.id, v.null), etc. matching the schema)
      2. handler async (ctx, args): construct ConvexRoleRepository(ctx.db) and/or ConvexUserRepository(ctx.db) (and ConvexTenantRepository for tenant operations); instantiate RoleService and/or UserService with the repos; build TenantContext via tenantContext(toTenantId(args.tenantId))
      3. call service.method(tCtx, args...) — for mutations that take Id<> args, run through toUserId/toRoleId mappers first per Pitfall 1
      4. map response — return plain strings (Convex serializes IDs automatically; branded types erase at runtime per RESEARCH.md Assumption A7)

    Per Pitfall 6: per-mutation DI is intentional; do NOT create a module-level service singleton. Per the convex/ HARD RULE: handlers MUST NOT enforce business rules — all uniqueness, cycle prevention, cross-tenant role rejection lives in the application service. Handlers only validate input shape, instantiate DI, call services, and map responses.

    Write tests/modules/directory/convex-role-repository.test.ts as ADAPTER MAPPING TESTS (not full Convex integration tests — D-20 defers full Convex runtime tests). Test the mappers and the repository's index-usage shape:
    - Round-trip: toRoleId(fromRoleId(roleId("r1"))) === roleId("r1") and the resulting string equals "r1"
    - roleDocToEntity({_id: "r1" as Id<"roles">, tenantId: "t1" as Id<"tenants">, name: "approver", createdAt: 1234}) returns Role with branded ids and createdAt
    - Spy/mock the MutationCtx.db: stub db.query to return an object with withIndex/unique/collect — assert ConvexRoleRepository.findByName calls .withIndex("by_tenant_name", builder) with a builder that calls q.eq("tenantId", ...) then q.eq("name", ...). The point of the test is to verify index NAMES and key sequences match the schema exactly. The mock can use Vitest vi.fn() to record argument shapes.

    Write tests/modules/directory/convex-user-repository.test.ts similarly. Assert findByEmail uses by_tenant_email index and the right q.eq sequence; assert userDocToEntity maps optional fields (undefined -> null) per Pitfall 5; round-trip ID tests.

    Note that adapters/convex/** files are excluded from coverage threshold per vitest.config.ts (set in plan 01-01: coverage.exclude includes "src/modules/**/adapters/convex/**"). Adapter coverage is satisfied by these targeted mapping tests, not threshold-enforced. The 90% threshold applies to application/ and adapters/memory/.

  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx eslint src/modules/directory/adapters/convex convex --max-warnings 0 &amp;&amp; npx vitest run tests/modules/directory/convex-role-repository.test.ts tests/modules/directory/convex-user-repository.test.ts --reporter=dot &amp;&amp; grep -F by_tenant_name src/modules/directory/adapters/convex/convex-role-repository.ts &amp;&amp; grep -F by_tenant_email src/modules/directory/adapters/convex/convex-user-repository.ts &amp;&amp; grep -F by_tenant_manager src/modules/directory/adapters/convex/convex-user-repository.ts &amp;&amp; grep -F RoleService convex/directory.ts &amp;&amp; grep -F UserService convex/directory.ts &amp;&amp; grep -F mutation convex/directory.ts &amp;&amp; grep -F query convex/directory.ts</automated>
  </verify>
  <done>
    - mappers.ts implements six ID mappers + three document-to-entity mappers; round-trip tests pass
    - ConvexRoleRepository implements all six RoleRepositoryPort methods and uses by_tenant_name index for every query (no .collect() without .withIndex)
    - ConvexUserRepository implements all eight UserRepositoryPort methods and uses by_tenant_email / by_tenant_role / by_tenant_manager indexes
    - ConvexTenantRepository implements TenantRepositoryPort
    - convex/directory.ts exports 14 handlers (5 role + 9 user) following Pattern 9; no business logic in convex/directory.ts; ESLint Zones 6 and 7 hold — domain entities NOT deep-imported (Zone 6) AND application/* services NOT deep-imported (Zone 7: RoleService/UserService/TenantContext/error types come through the @/modules/directory barrel). Adapter implementations (ConvexRoleRepository/ConvexUserRepository/ConvexTenantRepository) are deep-imported from src/modules/directory/adapters/convex/** for DI wiring, which is explicitly allowed by Zone 7 since these classes are not exposed through the barrel.
    - Adapter mapping tests verify index names and key sequences match convex/schema.ts exactly
    - `npx eslint convex --max-warnings 0` passes — convex/directory.ts respects the HARD RULE
    - All adapter mapping tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING — Push Convex schema to live deployment + run gitnexus analyze + final phase verification</name>
  <files>
    .env.local
  </files>
  <read_first>
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md "Environment Availability" (npx convex dev provisions deployment; npx convex deploy for CI with CONVEX_DEPLOY_KEY)
    - .planning/phases/01-core-platform-foundations/01-RESEARCH.md "State of the Art" + Assumption A6 (npx convex deploy is the production-deploy command; npx convex dev is interactive)
    - .planning/phases/01-core-platform-foundations/01-VALIDATION.md "Manual-Only Verifications" (Convex schema push reaches the live deployment — requires Convex CLI auth + dev deployment provisioned interactively)
    - CLAUDE.md "Keeping the Index Fresh" (npx gitnexus analyze after committing code; preserve embeddings if present via --embeddings flag)
    - convex/schema.ts (the schema being pushed)
    - .gitignore (must already include .convex/ and convex/_generated/ — set in plan 01-01)
  </read_first>
  <what-built>
    Convex schema (six tables, multiple composite indexes), domain layer (three modules), application services (RoleService, UserService), Convex adapters (mappers + three repositories), in-memory fakes, full Vitest test suite at 90%+ coverage on critical paths. All committed. What is NOT done: the schema is in convex/schema.ts but has not been pushed to a live Convex deployment. Until pushed, the Convex dashboard shows no tables and the application could never run against real persistence.
  </what-built>
  <how-to-verify>
    1. Run `npx convex dev` from the repo root. This will:
       a. Prompt for Convex login if not already authenticated (browser flow). Authenticate.
       b. Prompt to create a new project or select an existing one. Create a new project named "mini-stry" (or "mini-stry-dev").
       c. Provision a development deployment.
       d. Write CONVEX_DEPLOYMENT to .env.local.
       e. Push convex/schema.ts and regenerate convex/_generated/.
       f. Start the file watcher (Ctrl-C to exit after the initial push completes).

    2. Open the Convex Dashboard for the new deployment. In the "Data" tab, verify all six tables exist: tenants, users, roles, policies, policyVersions, auditLogs. In the "Functions" tab, verify 14 handlers from convex/directory.ts are listed (5 role + 9 user mutations/queries).

    3. Smoke-check a single end-to-end call via the Convex Dashboard "Functions" tab or `npx convex run`:
       a. Click "createTenant"... wait — Phase 1 does not have a createTenant Convex handler in convex/directory.ts (Tenant bootstrap is intentionally not exposed at Phase 1; tenant creation happens via Convex Auth in a later phase or via a one-off seeding script). Skip this step — the data tab verification is sufficient.
       b. Alternative: from the Dashboard Data tab, manually insert one row into the tenants table with name "smoke-test" and createdAt as the current epoch ms. Then invoke createRole(tenantId: <that tenant id>, name: "tester") via the Dashboard Functions tab. Verify the role appears in the roles table with tenantId pointing back to the smoke-test tenant.

    4. After exiting `npx convex dev` (Ctrl-C), commit .env.local IF (and only if) the project policy is to commit dev environment files (per .gitignore .env.local is gitignored — leave it gitignored; the developer keeps the CONVEX_DEPLOYMENT locally and CI uses CONVEX_DEPLOY_KEY).

    5. Run `npx gitnexus analyze` per CLAUDE.md to rebuild the code intelligence index. If .gitnexus/meta.json shows stats.embeddings > 0, use `npx gitnexus analyze --embeddings` to preserve them; if 0, omit the flag. Verify with `npx gitnexus analyze` exit code 0 and a new .gitnexus/ index timestamp.

    6. Final phase verification: run `npm test && npm run lint && npm run typecheck && npx vitest run --coverage` — all four exit 0 and coverage threshold (90% on all four metrics) is met.

    Report back to the planner with:
    - "approved" if all six steps succeeded
    - Or describe any failure (auth prompt issues, table missing, dashboard inaccessible, gitnexus error)

  </how-to-verify>
  <resume-signal>Type "approved" if the schema is pushed, all six tables are visible in the Convex Dashboard, gitnexus analyze succeeded, and the final test/lint/typecheck/coverage run is green. Otherwise describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                      | Description                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Application layer -> Persistence layer                        | Cross-tenant data leakage is the highest-severity threat in this phase. Multi-layer defense: TenantContext explicit on every method (D-19), tenant-scoped repository ports (D-09), tenant-prefixed composite indexes at schema (plan 01-03), TenantContext-aware in-memory fakes (this plan) |
| Role assignment -> User record                                | If UserService.assignRole accepts a roleId from another tenant, the user record holds a roleId pointing to a role that doesn't exist (or shouldn't be reachable) in their tenant. Privilege escalation surface                                                                               |
| Manager assignment -> Org graph                               | A cycle in managerId creates a denial-of-service vector for any future code that walks the chain (Phase 5 will use this heavily). Cycle prevention lives in UserService.setManager (D-11)                                                                                                    |
| convex/directory.ts -> application services                   | If convex/directory.ts contains business logic instead of pure DI, the HARD RULE is violated and tests at the application layer no longer reflect production behavior                                                                                                                        |
| Convex dev deployment provisioning -> developer local machine | First-time `npx convex dev` prompts for browser login and writes CONVEX_DEPLOYMENT to .env.local — local environment side effect                                                                                                                                                             |

## STRIDE Threat Register

| Threat ID | Category                                                                          | Component                                                                                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-1-MT-02 | Information Disclosure / Elevation of Privilege (CRITICAL — Multi-tenant leakage) | RoleService.findRoleById, RoleService.findRoleByName, RoleService.listRolesByTenant, UserService.findUserById/byEmail/listByTenant — service-layer methods | mitigate    | TenantContext is the first parameter on every method (D-19). The repository ports (defined in plan 01-02) accept TenantContext on every method. In-memory fakes (this plan Task 1) filter `entity.tenantId === ctx.tenantId` before returning. Convex adapters (this plan Task 2) call `.withIndex("by_tenant_*", q => q.eq("tenantId", fromTenantId(ctx.tenantId)))` on every query — the composite indexes from plan 01-03 make tenant filtering the cheapest path. Tests: tests/modules/directory/tenant-isolation.test.ts asserts cross-tenant reads return null for every method (this plan Task 1).                                                                                                                                                                                   |
| T-1-RP-01 | Elevation of Privilege — Role assignment privilege escalation                     | UserService.createUser, UserService.assignRole                                                                                                             | mitigate    | Both methods verify the roleId via roleRepo.findById(ctx, roleId) BEFORE persisting. Because roleRepo enforces tenant scoping, a roleId from a different tenant returns null and the service throws RoleNotFoundError. Test in tests/modules/directory/user-service.test.ts: createUser with cross-tenant roleId throws RoleNotFoundError; assignRole with cross-tenant roleId throws.                                                                                                                                                                                                                                                                                                                                                                                                      |
| T-1-MC-01 | DoS — Manager cycle / DoS                                                         | UserService.setManager                                                                                                                                     | mitigate    | Pattern 7 cycle detection walks the manager chain upward; rejects direct self-loop, indirect cycles, and chains exceeding MAX_MANAGER_CHAIN_DEPTH = 50. Test in tests/modules/directory/cycle-prevention.test.ts: covers self-loop, 2-cycle, N-cycle, broken chain (allowed), depth limit (51 throws).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| T-1-RU-02 | Tampering — Role name collision squatting                                         | RoleService.createRole, RoleService.renameRole                                                                                                             | mitigate    | RoleService.createRole calls findByName first; if exists, throws RoleNameAlreadyExistsError. RoleService.renameRole same. At the Convex adapter (Pattern 8 + Pitfall 2): Convex OCC re-runs concurrent mutations deterministically; the loser's findByName returns the now-existing role and throws. Test: role-service.test.ts asserts duplicate name within tenant rejected, same name in different tenant allowed.                                                                                                                                                                                                                                                                                                                                                                       |
| T-1-AT-02 | Tampering — Audit log mutation                                                    | (deferred — Phase 1 has no AuditLog ports or services)                                                                                                     | accept      | Plan 01-02 created the AuditLog entity but NO AuditLog repository port and NO AuditService. There is no API surface at Phase 1 to mutate audit logs. Phase 3+ wires audit logging; the AuditLog port at that time will omit update/delete methods (per Phase 1 threat model in CONTEXT.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T-1-BC-01 | Architectural — convex/ contains business logic                                   | convex/directory.ts                                                                                                                                        | mitigate    | Every handler follows Pattern 9 (DI wiring only). The handler body has: args validator, ConvexRoleRepository / ConvexUserRepository construction, RoleService / UserService construction, single service.method call, return mapping. No `if`, no validation beyond Convex args, no business decisions. ESLint Zones 6 and 7 enforce convex/ discipline: Zone 6 blocks domain entity deep imports (except domain/ids.ts); Zone 7 blocks application/\* deep imports and non-ids domain deep imports — convex/ must go through the module barrel for services, contexts, and error types. Adapters/convex/\*\* deep imports remain allowed for DI wiring (adapter classes are not barrel-exposed). Code review: any handler with > 10 lines of logic between DI and service call is suspect. |
| T-1-BD-01 | Branded ID confusion at boundary                                                  | convex/directory.ts handlers passing Convex Id<> values into service methods that expect branded IDs                                                       | mitigate    | Pitfall 1: every incoming Id<> arg in convex/directory.ts goes through toTenantId/toUserId/toRoleId mappers before being passed to a service method. Verified by adapter mapping tests (Task 2) and by ESLint configuration (services and domain cannot import from convex/\_generated).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| T-1-CV-01 | Spoofing — Schema push to wrong deployment                                        | Task 3 first-time `npx convex dev`                                                                                                                         | mitigate    | First-run is interactive (developer selects project + deployment name); subsequent runs use CONVEX_DEPLOYMENT from .env.local. CI uses CONVEX_DEPLOY_KEY scoped to the production deployment. The first-run interactive prompt prevents accidental deploy to a stranger's project.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

No high-severity threats remain unmitigated. T-1-MT-02 (multi-tenant leakage) is the keystone threat for the entire Phase 1 architecture; mitigation is layered across schema (plan 01-03 tenant-prefixed indexes), application ports (plan 01-02 TenantContext-first), application services + repositories (this plan), and lint (plan 01-01 module boundary zones). The redundancy is intentional; any single layer breaking still leaves the others as defense.
</threat_model>

<deferred*verification>
Per D-20 in CONTEXT.md, end-to-end cross-tenant denial verification at the live Convex layer is intentionally deferred for Phase 1. The defense surface relies on composition rather than a live integration test: (1) schema-level tenant-prefixed indexes (plan 01-03) make tenant filtering the cheapest query path; (2) service-level TenantContext-first methods (D-19) make tenant scope explicit in every signature; (3) adapter-level belt-and-braces tenantId equality check inside ConvexRoleRepository.findById and ConvexUserRepository.findById defends against schema regressions. The in-memory tenant-isolation test suite (tests/modules/directory/tenant-isolation.test.ts) plus the adapter mapping tests (convex-role-repository.test.ts, convex-user-repository.test.ts asserting `.withIndex("by_tenant*\*", q => q.eq("tenantId", ...))`) plus the BLOCKING human-verify smoke (Task 3 step 3) is the agreed-upon Phase 1 trade-off. Full Convex integration tests with two real tenants writing/reading concurrently are deferred to Phase 2 once the Convex deployment exists and convex-test infrastructure is wired.

Also accepted as a deliberate trade-off (per checker Issue 4 advisory): Task 1 touches 12 files in a single cohesive unit (RoleService + UserService + their in-memory fakes + the test helpers consuming both + the four behavior test files exercising both services together). Splitting along service boundaries would force duplicate setup helpers and cross-task dependencies on a shared `setupDirectory()` fixture; the current grouping keeps the service-test-fake triangle in one editable unit, which matches how plans 01-02 and 01-03 structured their related-artifact tasks.
</deferred_verification>

<verification>
After all three tasks land:
- `npm test` exits 0 (all Vitest suites green)
- `npx vitest run --coverage` exits 0 AND coverage thresholds met on lines/branches/functions/statements >= 90% for src/modules/directory/{application,adapters/memory}
- `npm run lint` exits 0 — zones in eslint.config.js hold; no domain imports into convex/ beyond domain/ids.ts; no application/* importing adapters/*
- `npm run typecheck` exits 0
- Convex Dashboard shows six tables (tenants, users, roles, policies, policyVersions, auditLogs) — verified manually in checkpoint Task 3
- `npx gitnexus analyze` returns clean and the new .gitnexus/meta.json has stats reflecting the just-landed code
- `git status` is clean except for .env.local (gitignored)
</verification>

<success_criteria>

- Phase 1 requirements CON-01, CON-02, CON-03, CON-04 each have a corresponding behavior test in the Vitest suite with passing status (POL-05/06 and AUD-03 are skeleton-only and satisfied by plan 01-02 entity interfaces + plan 01-03 schema)
- 90% coverage threshold met on critical paths (tenant isolation, role uniqueness, manager assignment + cycle prevention, role lookup, user lookup) per D-20 + engineering.md
- All eight UserService D-17 methods and all six RoleService D-17 methods implemented and tested
- Convex deployment provisioned and schema pushed (BLOCKING checkpoint)
- gitnexus index refreshed per CLAUDE.md
- Final test/lint/typecheck/coverage run all green; phase ready for /gsd:verify-work
  </success_criteria>

<output>
Create `.planning/phases/01-core-platform-foundations/01-04-SUMMARY.md` when done. Record: the implementation tradeoffs vs RESEARCH.md (any deviations), the actual coverage percentages reported by vitest --coverage on the critical path files, the list of Convex handlers wired in convex/directory.ts (14 expected), output of `npx convex dev` first-run (deployment name, tables verified visible in dashboard), confirmation that gitnexus analyze succeeded, and the final green-build summary line (test + lint + typecheck + coverage all 0 exit).
</output>
