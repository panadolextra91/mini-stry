---
phase: 05-approval-routing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/modules/approval/application/approval-routing-service.ts
  - src/modules/approval/application/approval-audit-subscriber.ts
  - src/modules/approval/adapters/convex/convex-approval-chain-repository.ts
  - src/modules/approval/adapters/convex/convex-approval-task-repository.ts
  - src/modules/approval/adapters/convex/mappers.ts
  - src/modules/approval/index.ts
  - src/modules/request/adapters/convex/convex-request-evaluation-repository.ts
  - src/modules/request/adapters/convex/mappers.ts
  - convex/schema.ts
  - convex/request.ts
  - tests/_helpers/in-memory-fakes.ts
  - tests/_helpers/manager-hierarchy-fixture.ts
  - tests/modules/approval/routing-service.test.ts
  - tests/modules/approval/approval-audit-subscriber.test.ts
autonomous: true
requirements: [DEC-03]

must_haves:
  truths:
    - "A request-approval Decision materializes exactly one ApprovalChain + one ApprovalTask (stageNumber=1); non-request-approval decisions are ignored"
    - "The manager walk starts at requester.managerId (self excluded) and resolves the first ancestor holding targetRoleId; no holder → RoutingError, no task created"
    - "A null requesterId → RoutingError before any walk (no requester to anchor on); ApprovalRoutingFailed emitted; no chain"
    - "Missing targetRoleId in the Role registry → RoleNotFoundError before any walk (SC#3); >50 hops → HierarchyTraversalError"
    - "act() rejects non-approvers (UnauthorizedApproverError) and terminal tasks (TaskAlreadyResolvedError); valid action drives deterministic task + chain transitions"
    - "Routing failures emit ApprovalRoutingFailed and never rethrow, so the persisted RequestEvaluation survives (D-54)"
    - "Approve/Reject and routing-failure events are audited by-reference (approval.task_approved / approval.task_rejected / approval.routing_failed)"
    - "Re-emitting the same evaluationRecordId produces exactly one chain (idempotency)"
    - "approvalChains + approvalTasks tables and requestEvaluations.requesterId exist on the live Convex deployment"
    - "submitRequest carries an actorId arg and builds tenantContext(tenantId, userId(actorId)) so the live RequestEvaluated subscription has a non-null requester to route on"
  artifacts:
    - path: "src/modules/approval/application/approval-routing-service.ts"
      provides: "onRequestEvaluated (walk + materialize + idempotency + failure-swallow) and act() guards"
      contains: "onRequestEvaluated"
    - path: "src/modules/approval/application/approval-audit-subscriber.ts"
      provides: "by-reference audit handlers for approval.* events (D-53)"
      contains: "approval.task_approved"
    - path: "convex/schema.ts"
      provides: "approvalChains + approvalTasks tables + requestEvaluations.requesterId"
      contains: "approvalChains"
    - path: "tests/_helpers/manager-hierarchy-fixture.ts"
      provides: "managerId/roleId graph builder → expected approver tuples"
  key_links:
    - from: "convex/request.ts"
      to: "ApprovalRoutingService.onRequestEvaluated"
      via: "requestDispatcher.on(\"RequestEvaluated\", e => routingService.onRequestEvaluated(tCtx, e))"
      pattern: "on\\(\"RequestEvaluated\""
    - from: "convex/request.ts"
      to: "tenantContext(tenantId, userId(actorId))"
      via: "submitRequest actorId arg threaded into the ctx the subscription uses"
      pattern: "userId\\(args\\.actorId\\)"
    - from: "src/modules/approval/application/approval-routing-service.ts"
      to: "userRepo.findById (manager walk)"
      via: "bounded cursor loop over managerId"
      pattern: "managerId"
    - from: "src/modules/approval/application/approval-routing-service.ts"
      to: "chainRepo.findByRequestEvaluationId"
      via: "idempotency pre-check before materialize"
      pattern: "findByRequestEvaluationId"
---

<objective>
Implement the core of the Decision Consumer: `ApprovalRoutingService` (`onRequestEvaluated` resolver/materializer + `act()` state-machine driver), the `approval.*` by-reference audit subscriber, the Convex persistence adapters + mappers + schema tables (incl. the `requestEvaluations.requesterId` column ripple into the Convex request adapter), the thin Convex DI wiring that subscribes routing to `"RequestEvaluated"` (and threads a live `actorId` so routing actually fires), the test-helper `setupApproval` + manager-hierarchy fixture, and the two service-level test suites. Finishes with a LIVE Convex schema push so the new tables/indexes are real (Vitest fakes cannot catch a missing table).

Purpose: This plan delivers DEC-03 — converting a Request-Approval Decision into a persisted, immutable single-task approval chain with a deterministic Approve/Reject machine, audited by-reference, with routing failures isolated from request submission (SC#1 + D-54). It builds entirely against the contracts established in 05-01.

Output: A working, 100%-covered (domain+application+memory) `ApprovalRoutingService` + audit subscriber; Convex adapters + schema for `approvalChains`/`approvalTasks` + `requesterId`; routing subscribed at the composition root with a non-null live actor source; and a verified live Convex deployment.

Scope decision — live `act()` handler deferred to Phase 6 (WARNING #4): This phase delivers the `act()` Approve/Reject SERVICE + its full state-machine + guard test coverage (SC#5), but does NOT add a live `approveTask`/`rejectTask` Convex mutation. CONTEXT defers the approver inbox UI to Phase 6; the live mutation belongs with that UI (it needs the inbox's actor/task context). `act()` is therefore exercised by fakes-only tests this phase. This is a deliberate, stated deferral, not an omission — the service contract is complete and Phase 6 wires the thin mutation over it.
</objective>

<execution_context>
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-approval-routing/05-CONTEXT.md
@.planning/phases/05-approval-routing/05-PATTERNS.md
@.planning/phases/05-approval-routing/05-VALIDATION.md
@.planning/phases/05-approval-routing/05-01-SUMMARY.md

<interfaces>
<!-- Established by 05-01 — build against these, do NOT redefine. Read the approval barrel + ports. -->

From @/modules/approval (05-01 barrel):
  ApprovalChain, ApprovalTask, ApprovalChainId, ApprovalTaskId, approvalChainId, approvalTaskId,
  ChainStatus ("IN_PROGRESS"|"APPROVED"|"REJECTED"), TaskState ("PENDING"|"APPROVED"|"REJECTED"),
  ApprovalEventMap, ApprovalTaskApprovedEvent, ApprovalTaskRejectedEvent, ApprovalRoutingFailedEvent,
  transitionTask, deriveChainStatus,
  RoutingError, HierarchyTraversalError, TaskAlreadyResolvedError, UnauthorizedApproverError,
  ApprovalChainRepositoryPort, ApprovalTaskRepositoryPort, CreateApprovalChainInput, CreateApprovalTaskInput,
  InMemoryApprovalChainRepository, InMemoryApprovalTaskRepository, EventDispatcher.

From @/modules/directory (existing barrel):
  TenantContext, tenantContext (now (tenantId, actorId?)), userId (factory), UserId, RoleId, User, Role,
  UserRepositoryPort, RoleRepositoryPort, RoleNotFoundError.  // reuse RoleNotFoundError — do NOT redefine
  NOTE: findById(ctx, id: UserId) requires a NON-NULL UserId (see ports/user-repository.port.ts line 7).
        requesterId is UserId|null on the entity, so the null case MUST be handled BEFORE calling userRepo.findById.

The Convex directory repos are NOT on the directory barrel — they are deep-imported (mirroring convex/directory.ts):
  ConvexUserRepository — src/modules/directory/adapters/convex/convex-user-repository.js — constructor (db)
  ConvexRoleRepository — src/modules/directory/adapters/convex/convex-role-repository.js — constructor (db)

From @/modules/request (existing barrel):
  RequestEvaluationId, RequestEvaluation (now has requesterId: UserId|null),
  RequestEvaluationRepositoryPort, RequestEventMap, ConvexRequestEvaluationRepository.

From @/modules/runtime (READ-ONLY barrel, SC#1):
  Decision  — request-approval carries targetRoleId: RoleId.

EventDispatcher (05-01, hardened): emit isolates+records per-handler failures via optional onError hook; sequential for...await preserved.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ApprovalRoutingService — onRequestEvaluated (walk + materialize + idempotency + failure-swallow) and act() guards, with manager-hierarchy fixture + setupApproval helper + routing-service tests</name>
  <files>src/modules/approval/application/approval-routing-service.ts, tests/_helpers/in-memory-fakes.ts, tests/_helpers/manager-hierarchy-fixture.ts, tests/modules/approval/routing-service.test.ts</files>
  <behavior>
    - request-approval Decision with a resolvable approver → exactly one ApprovalChain (status IN_PROGRESS) + one ApprovalTask (stageNumber 1, state PENDING, approverId = resolved user, approverRoleId = targetRoleId)
    - decision.kind !== "request-approval" (auto-approve / auto-reject) → no chain, no task (ignored)
    - null requester: the persisted RequestEvaluation.requesterId is null → RoutingError("no requester") thrown BEFORE any userRepo.findById walk; ApprovalRoutingFailed emitted; NO chain and NO task created; does not rethrow. (This is the DEC-03 null-path guard that also keeps tsc green: userRepo.findById is only ever called with a non-null UserId.)
    - walk hit: first ancestor (from requester.managerId upward) holding targetRoleId becomes approverId
    - walk miss: chain of managers ends with no holder → RoutingError, NO chain and NO task created; ApprovalRoutingFailed emitted; submit-equivalent does not throw
    - self-exclusion: requester themself holds targetRoleId but walk starts at requester.managerId → requester is NOT selected
    - missing targetRoleId in Role registry → RoleNotFoundError BEFORE any walk; ApprovalRoutingFailed emitted; no task
    - depth cap: >50 manager hops → HierarchyTraversalError; ApprovalRoutingFailed emitted; no task
    - idempotency: emitting the same evaluationRecordId twice → exactly one chain (second call no-ops via findByRequestEvaluationId)
    - act() valid APPROVE by the resolved approver → task PENDING→APPROVED, chain IN_PROGRESS→APPROVED, ApprovalTaskApproved emitted
    - act() valid REJECT → task PENDING→REJECTED, chain →REJECTED, ApprovalTaskRejected emitted
    - act() by a non-approver (ctx.actorId !== task.approverId) → UnauthorizedApproverError, no state change, no event
    - act() on a terminal task → TaskAlreadyResolvedError, no state change, no event
    - immutability (D-44): mutating the user's managerId AFTER chain creation does not change the stored task.approverId
    - cross-tenant (CON-01): chain/task reads under a different ctx.tenantId return null
  </behavior>
  <read_first>
    - src/modules/request/application/policy-runtime-service.ts (constructor-injected ports + dispatcher.emit shape, lines 11-17 + 62-66; 05-PATTERNS.md "approval-routing-service.ts" section, lines 256-314)
    - src/modules/directory/application/user-service.ts lines 69-110 (the bounded manager-walk template: MAX_MANAGER_CHAIN_DEPTH=50, cursor loop, findById, break-on-missing, depth increment, throw-on-cap)
    - src/modules/directory/ports/user-repository.port.ts line 7 (findById(ctx, id: UserId) — id is NON-NULL UserId; this is WHY the null-requester branch must precede the walk)
    - src/modules/runtime/domain/decision.ts lines 6-9 (READ-ONLY Decision union — request-approval.targetRoleId; do NOT modify, SC#1)
    - src/modules/directory/application/errors.ts (RoleNotFoundError — reuse via directory barrel, do NOT redefine)
    - src/modules/approval/application/errors.ts + domain/state-machine.ts (05-01 outputs — the errors + transitionTask/deriveChainStatus this service calls)
    - tests/_helpers/in-memory-fakes.ts (setupRequest lines 42-65 + setupDirectory; 05-PATTERNS.md "in-memory-fakes.ts" section, lines 429-431)
    - tests/_helpers/tenant-context-fixture.ts (TENANT_A + tenantContext usage)
    - tests/modules/request/request-audit-subscriber.test.ts (test file shape; 05-PATTERNS.md "test files" section)
    - 05-CONTEXT.md D-43, D-44, D-49, D-50, D-51, D-52, D-54; 05-RESEARCH.md Patterns 3-5, Pitfall 3 (record don't swallow), Pitfall 5 (idempotency), Open-Q2/A3 (swallow own routing errors, never rethrow)
  </read_first>
  <action>
    approval-routing-service.ts — constructor-injected ports ONLY (D-18, never `new`s an adapter): chainRepo (ApprovalChainRepositoryPort), taskRepo (ApprovalTaskRepositoryPort), userRepo (UserRepositoryPort, directory barrel), roleRepo (RoleRepositoryPort, directory barrel), evalRepo (RequestEvaluationRepositoryPort, request barrel), dispatcher (EventDispatcher<ApprovalEventMap>). All methods take `ctx: TenantContext` first (D-19).

    onRequestEvaluated(ctx, event): WIDEN the try/catch to cover the WHOLE body so EVERY failure mode (incl. the pre-walk repo reads in steps 1-2) emits ApprovalRoutingFailed (WARNING #2 — do NOT leave steps 1-2 outside the try). Order exactly, all inside one try: (1) `chainRepo.findByRequestEvaluationId(ctx, event.evaluationRecordId)`; if non-null, RETURN (idempotency no-op, Pitfall 5). (2) `evalRepo.findById(ctx, event.evaluationRecordId)` → read `decision` + `requesterId`. (3) if `decision?.kind !== "request-approval"` → RETURN (ignore; Decision union from runtime barrel, read-only). (4) NULL-REQUESTER GUARD (DEC-03 null path + tsc safety): if `requesterId === null` → `throw new RoutingError(...)` with reason "no requester" — this runs BEFORE any userRepo.findById so the subsequent walk only ever receives a non-null UserId (the entity field is UserId|null but the port's findById demands UserId). (5) `roleRepo.findById(ctx, decision.targetRoleId)` → null → throw RoleNotFoundError (SC#3, BEFORE the walk). (6) resolve requester via `userRepo.findById(ctx, requesterId)` (requesterId is now narrowed to UserId by the step-4 guard); walk managerId chain (self excluded — start at requester.managerId) with MAX_HIERARCHY_DEPTH=50: cap exceeded → HierarchyTraversalError; broken link (findById null) → break → RoutingError; first ancestor with `roleId === targetRoleId` → approver (D-43/D-49/D-50). (7) materialize immutable snapshot (D-44): `chainRepo.create(ctx, { requestEvaluationId, status: "IN_PROGRESS" })` then `taskRepo.create(ctx, { chainId, stageNumber: 1, approverId, approverRoleId: targetRoleId, state: "PENDING" })`. CATCH (covering ALL of steps 1-7): on RoutingError | RoleNotFoundError | HierarchyTraversalError → `dispatcher.emit("ApprovalRoutingFailed", { tenantId, evaluationRecordId, reason, timestamp })` and DO NOT rethrow (Open-Q2/A3 — keeps the persisted RequestEvaluation valid; the hardened dispatcher from 05-01 is the defense-in-depth second layer). reason = err.message. (Because step 1's idempotency RETURN is inside the try, ensure the early returns happen before the create — a RETURN is not a throw, so it short-circuits without emitting; that is correct.)

    act(ctx, taskId, action: "APPROVE"|"REJECT"): (1) `task = taskRepo.findById(ctx, taskId)` (null → TaskAlreadyResolvedError or a not-found guard — pick per analog, document). (2) GUARD ORDER per D-52: `if (task.state !== "PENDING") throw TaskAlreadyResolvedError(taskId)` then `if (ctx.actorId !== task.approverId) throw UnauthorizedApproverError(...)`. (3) `next = transitionTask(task.state, action)` (pure). (4) `taskRepo.updateState(ctx, taskId, next)`. (5) `chainStatus = deriveChainStatus([next])` (one task in Phase 5). (6) `chainRepo.updateStatus(ctx, task.chainId, chainStatus)`. (7) `dispatcher.emit(action==="APPROVE" ? "ApprovalTaskApproved" : "ApprovalTaskRejected", { tenantId: ctx.tenantId, taskId, chainId: task.chainId, actorId: ctx.actorId!, timestamp: Date.now() })`. act() reads the STORED approverId — NEVER re-walks the hierarchy (D-44, anti-pattern). NOTE: act() has no live Convex handler this phase — it is service + tests only; the thin approveTask/rejectTask mutation is deferred to Phase 6 with the inbox UI (see objective scope decision).

    manager-hierarchy-fixture.ts (test helper): a builder that constructs a graph of Users with managerId/roleId links and returns the users + expected approver tuples for the walk-hit / walk-miss / self-exclusion / deep-chain (>50) scenarios. Use directory barrel factories (userId(...), roleId(...)).

    in-memory-fakes.ts: add `setupApproval` (mirror setupRequest lines 42-65) composing chain/task fakes (from 05-01 memory adapters) + directory userRepo/roleRepo (from setupDirectory) + evalRepo (from setupRequest) + an ApprovalEventMap dispatcher; instantiate ApprovalRoutingService; wire `requestDispatcher.on("RequestEvaluated", e => routingService.onRequestEvaluated(ctx, e))` and the approval audit subscriber (added in Task 2; if Task 2 not yet merged, leave a TODO seam — but since both tasks are in this plan, wire it). Return the service + repos + dispatchers + auditRepo for assertions.

    routing-service.test.ts: cover EVERY <behavior> case above using setupApproval + the hierarchy fixture + TENANT_A, INCLUDING the null-requester case (persist a RequestEvaluation with requesterId=null, emit, assert NO chain + ApprovalRoutingFailed emitted + no throw). Include the cross-tenant case (read under a second tenant ctx → null) and the immutability case (mutate managerId post-creation, assert stored approverId unchanged). Target 100% coverage of approval-routing-service.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/modules/approval/routing-service.test.ts --reporter=dot && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/modules/approval/routing-service.test.ts` green with all 15 behavior cases asserted (walk hit/miss/self-exclusion, null-requester, RoleNotFound, depth cap, idempotency, ignore-non-approval, act approve/reject/unauthorized/terminal, immutability, cross-tenant).
    - `grep -n "requesterId === null\|requesterId == null\|=== null" src/modules/approval/application/approval-routing-service.ts` matches a null-requester guard that throws RoutingError BEFORE the first `userRepo.findById` call (the guard precedes the walk in source order).
    - `grep -n "findByRequestEvaluationId" src/modules/approval/application/approval-routing-service.ts` matches BEFORE any create call (idempotency pre-check).
    - The whole `onRequestEvaluated` body (steps 1-7) is inside ONE try/catch — `chainRepo.findByRequestEvaluationId` and `evalRepo.findById` are inside the try (WARNING #2): the `try {` opening precedes the `findByRequestEvaluationId` call in source order.
    - `grep -n "ApprovalRoutingFailed" src/modules/approval/application/approval-routing-service.ts` matches inside a catch that does NOT rethrow (`grep -n "throw" ` shows no rethrow of RoutingError/RoleNotFoundError/HierarchyTraversalError after emit).
    - `grep -n "managerId" src/modules/approval/application/approval-routing-service.ts` matches; `grep -n "MAX_HIERARCHY_DEPTH\|>= 50\|>= MAX" src/modules/approval/application/approval-routing-service.ts` shows the depth cap.
    - act() must not call the walk: `grep -n "managerId" ` appears only in onRequestEvaluated's resolver, not in act().
    - act() guard order: `task.state !== "PENDING"` check precedes the `ctx.actorId !== task.approverId` check (D-52).
    - `npx tsc --noEmit` exits 0 (proves the null-requester guard narrows requesterId so `userRepo.findById(ctx, requesterId)` typechecks against the non-null `UserId` param).
  </acceptance_criteria>
  <done>ApprovalRoutingService resolves + materializes immutable single-task chains, enforces auth/idempotency/terminal/null-requester guards, swallows its own routing errors into ApprovalRoutingFailed (D-54) with the FULL body inside one try/catch, and is exercised to 100% by routing-service.test.ts via setupApproval + the hierarchy fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Approval by-reference audit subscriber + barrel export + audit-subscriber tests</name>
  <files>src/modules/approval/application/approval-audit-subscriber.ts, src/modules/approval/index.ts, tests/modules/approval/approval-audit-subscriber.test.ts</files>
  <behavior>
    - on ApprovalTaskApproved → AuditLog created with eventType "approval.task_approved" and payload carrying ONLY ids/metadata { taskId, chainId, actorId } (+ timestamp), never content
    - on ApprovalTaskRejected → eventType "approval.task_rejected", by-reference payload
    - on ApprovalRoutingFailed → eventType "approval.routing_failed", payload { evaluationRecordId, reason } by-reference
    - subscriber uses ctx = { tenantId: event.tenantId } and calls auditRepo.create(ctx, { eventType, payload })
  </behavior>
  <read_first>
    - src/modules/audit/application/request-audit-subscriber.ts lines 13-53 (constructor (auditRepo, dispatcher); dispatcher.on("Event", async e => auditRepo.create({tenantId:e.tenantId}, {eventType:"<aggregate>.<action>", payload:{...ids only}})); 05-PATTERNS.md "Event dispatch + by-reference audit" section, lines 444-446)
    - src/modules/audit/ports/audit-log-repository.port.ts lines 9-12 (AuditLogRepositoryPort.create(ctx, { eventType, payload }))
    - src/modules/audit/index.ts (barrel export pattern for subscribers)
    - src/modules/approval/domain/approval-events.ts (05-01 — the ApprovalEventMap to subscribe to)
    - tests/modules/request/request-audit-subscriber.test.ts (test shape + how it asserts by-reference payload)
    - 05-CONTEXT.md D-37 (by-reference, never content), D-53 (approval.* eventTypes + payload), D-54 (routing_failed audited)
  </read_first>
  <action>
    approval-audit-subscriber.ts: class `ApprovalAuditSubscriber` mirroring request-audit-subscriber.ts — constructor `(auditRepo: AuditLogRepositoryPort, dispatcher: EventDispatcher<ApprovalEventMap>)`; in the constructor register three handlers via `dispatcher.on(...)`: `ApprovalTaskApproved` → eventType `"approval.task_approved"`, `ApprovalTaskRejected` → `"approval.task_rejected"`, `ApprovalRoutingFailed` → `"approval.routing_failed"`. Each handler builds `ctx = { tenantId: event.tenantId }` and calls `auditRepo.create(ctx, { eventType, payload })` where payload carries ONLY identifiers/metadata (taskId/chainId/actorId or evaluationRecordId/reason) — NEVER request/decision content (D-37/D-53). AuditLogRepositoryPort + AuditLog come from the audit barrel; ApprovalEventMap from the approval domain. Decide placement: this subscriber lives in approval/application (it depends on ApprovalEventMap) and imports the audit port via the audit barrel — confirm no ESLint zone violation (audit port is exported from @/modules/audit).

    index.ts (approval barrel): add the `ApprovalRoutingService` (from Task 1) and `ApprovalAuditSubscriber` exports under the Application services group, plus the Convex adapter exports added in Task 3 (or add those in Task 3). Keep grouped-comment structure from 05-01.

    approval-audit-subscriber.test.ts: emit each approval event through a dispatcher wired to the subscriber + an in-memory auditRepo fake; assert the AuditLog row's eventType string and that payload contains ONLY the by-reference ids (assert NO request/decision content keys present). Cover all three event types.
  </action>
  <verify>
    <automated>npx vitest run tests/modules/approval/approval-audit-subscriber.test.ts --reporter=dot && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "approval.task_approved\|approval.task_rejected\|approval.routing_failed" src/modules/approval/application/approval-audit-subscriber.ts` shows all three eventType strings.
    - `npx vitest run tests/modules/approval/approval-audit-subscriber.test.ts` green; tests assert by-reference payload (ids only) for all three events and assert NO content fields.
    - `grep -n "ApprovalAuditSubscriber\|ApprovalRoutingService" src/modules/approval/index.ts` shows both exported.
    - `npm run lint` passes (subscriber's cross-module import of the audit port goes through the audit barrel — no deep import / zone violation).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Approve/Reject/routing-failure events are persisted as by-reference AuditLog entries with the approval.* eventType convention (D-53/D-54); the subscriber + routing service are exported from the approval barrel.</done>
</task>

<task type="auto">
  <name>Task 3: Convex adapters + mappers + schema (approvalChains/approvalTasks + requesterId), thin DI wiring/subscription with live actor source, and LIVE Convex push</name>
  <files>src/modules/approval/adapters/convex/convex-approval-chain-repository.ts, src/modules/approval/adapters/convex/convex-approval-task-repository.ts, src/modules/approval/adapters/convex/mappers.ts, src/modules/approval/index.ts, src/modules/request/adapters/convex/convex-request-evaluation-repository.ts, src/modules/request/adapters/convex/mappers.ts, convex/schema.ts, convex/request.ts</files>
  <read_first>
    - GitNexus MANDATE: before editing convex/request.ts wiring, run `gitnexus_impact({target: "submitRequest", direction: "upstream"})` (or the handler symbol) and report. Run `gitnexus_detect_changes()` before the commit.
    - convex/request.ts (whole file — HARD-RULE header; it currently builds ONLY policyRepo/versionRepo/evalRepo/auditRepo and a requestDispatcher with `void new RequestAuditSubscriber` at line ~29; it builds `tenantContext(tenantId(args.tenantId))` at line 51 with NO actorId; args are { tenantId, requestType, context }. You WILL add an actorId arg + construct the directory + approval repos here.)
    - convex/directory.ts lines 9-10 + 27/54-55 (the EXACT deep-import paths + construction pattern for ConvexUserRepository/ConvexRoleRepository — these are NOT on the @/modules/directory barrel, so import them by file path the same way: `../src/modules/directory/adapters/convex/convex-user-repository.js` and `convex-role-repository.js`, each `new X(ctx.db)`)
    - src/modules/request/adapters/convex/convex-request-evaluation-repository.ts (whole file — db type, insert-then-get, tenant guard line ~35, withIndex pattern; 05-PATTERNS.md "convex-*-repository.ts" section)
    - src/modules/request/adapters/convex/mappers.ts (toX/fromX ID bridge lines 11-14, entity mapper casting string cols, the requesterId ripple per 05-PATTERNS.md "request-evaluation ... convex" bullet)
    - src/modules/directory/adapters/convex/mappers.ts lines 31-43 (nullable `doc.x ? toId(doc.x) : null` pattern for requesterId)
    - convex/schema.ts (requestEvaluations table lines ~80-93 + the D-09 tenant-prefixed-index header comment; 05-PATTERNS.md "convex/schema.ts" section, exact table defs lines 393-415)
    - convex/_branded.ts (brandedString validators — likely NO change needed; new IDs use v.id(...) or v.string())
    - 05-CONTEXT.md D-09 (tenant-prefixed + reverse-lookup indexes), convex/ HARD RULE; 05-RESEARCH Runtime State Inventory A1 (requesterId nullable for pre-existing rows), Pitfall 6 (no logic in convex/)
  </read_first>
  <action>
    convex/schema.ts: extend `requestEvaluations` with `requesterId: v.union(v.string(), v.null())` (nullable — pre-existing Phase 4 rows lack it, A1). Add two tables after it, every index tenant-prefixed (D-09): `approvalChains` = { tenantId: v.id("tenants"), requestEvaluationId: v.id("requestEvaluations"), status: v.string(), createdAt: v.number() } with `.index("by_tenant_created", ["tenantId","createdAt"])` and `.index("by_tenant_request_evaluation", ["tenantId","requestEvaluationId"])` (reverse lookup + idempotency). `approvalTasks` = { tenantId: v.id("tenants"), chainId: v.id("approvalChains"), stageNumber: v.number(), approverId: v.string(), approverRoleId: v.string(), state: v.string(), createdAt: v.number() } with `.index("by_tenant_chain", ["tenantId","chainId"])` and `.index("by_tenant_approver", ["tenantId","approverId"])` (Phase 6 inbox readiness). requestEvaluationId/chainId use v.id(...) (cross-table refs); approverId/approverRoleId are v.string() (branded strings, like policyVersions.createdBy).

    Request Convex ripple (D-47/D-48): in convex-request-evaluation-repository.ts add `requesterId: input.requesterId` to the insert; in request mappers.ts add `requesterId: doc.requesterId ? buildUserId(doc.requesterId) : null` to the entity mapper (nullable pattern). (Entity/port/in-memory ripple already done in 05-01; this closes the Convex side.)

    approval Convex adapters (mirror convex-request-evaluation-repository.ts): constructor takes `db: MutationCtx["db"] | QueryCtx["db"]`; `create` guards `if (!('insert' in this.db)) throw ...`, inserts via fromX mappers, re-`get`s and runs toXDomain; `findById` applies the tenant guard `if (doc.tenantId !== fromTenantId(ctx.tenantId)) return null`; reverse-lookup reads use `.withIndex(...)` — chain `findByRequestEvaluationId` → `by_tenant_request_evaluation`; task `findByChainId` → `by_tenant_chain`. `updateStatus`/`updateState` patch the doc and re-read. NO branching/walk logic in adapters.

    approval mappers.ts: toX/fromX ID bridges for ApprovalChainId/ApprovalTaskId (v.id-backed); entity mappers cast `doc.status as ChainStatus`, `doc.state as TaskState`, and rebrand `approverId`/`approverRoleId` via buildUserId/buildRoleId (stored as plain strings). Mirror request + directory mappers.

    approval/index.ts: export the two Convex repositories (so convex/ DI can construct them) under the Convex adapters group.

    convex/request.ts (composition root — thin DI ONLY, Pitfall 6 — RESOLVE the live actor source, BLOCKER 1b):
      (a) ADD an `actorId: v.string()` arg to the `submitRequest` mutation args (alongside tenantId/requestType/context). Import `userId` from `../src/modules/directory/index.js` (it is barrel-exported). Change the ctx build at line ~51 to `const tCtx = tenantContext(tenantId(args.tenantId), userId(args.actorId));` so the live RequestEvaluated subscription has a non-null requester to anchor routing on (without this, every persisted requesterId is null and routing would only ever emit ApprovalRoutingFailed). The submit() service already threads `ctx.actorId ?? null` into requesterId (05-01 Task 1), so this single ctx change makes the live path materialize chains.
      (b) Deep-import the directory Convex repos exactly as convex/directory.ts does (they are NOT on the barrel): `import { ConvexUserRepository } from "../src/modules/directory/adapters/convex/convex-user-repository.js";` and `import { ConvexRoleRepository } from "../src/modules/directory/adapters/convex/convex-role-repository.js";`. Import the approval pieces (ApprovalRoutingService, ApprovalAuditSubscriber, ConvexApprovalChainRepository, ConvexApprovalTaskRepository, ApprovalEventMap) from `../src/modules/approval/index.js`.
      (c) Inside the SAME `submitRequest` mutation (Open-Q1/Q2 — routing shares ctx.db / the transaction), after the existing RequestAuditSubscriber wiring, CONSTRUCT (submitRequest does NOT currently build these — WARNING #3): `const userRepo = new ConvexUserRepository(ctx.db);`, `const roleRepo = new ConvexRoleRepository(ctx.db);`, `const chainRepo = new ConvexApprovalChainRepository(ctx.db);`, `const taskRepo = new ConvexApprovalTaskRepository(ctx.db);` (reuse the existing `evalRepo`), `const approvalDispatcher = new EventDispatcher<ApprovalEventMap>();`, `void new ApprovalAuditSubscriber(auditRepo, approvalDispatcher);`, `const routingService = new ApprovalRoutingService(chainRepo, taskRepo, userRepo, roleRepo, evalRepo, approvalDispatcher);`. Then subscribe `requestDispatcher.on("RequestEvaluated", (e) => routingService.onRequestEvaluated(tCtx, e));` (use the actorId-bearing tCtx from (a)). Because the routing service swallows its own routing errors (Task 1), the submit mutation still commits even on routing failure (D-54). request/ keeps ZERO import of approval/ (SC#1 holds structurally — the subscription is wired in convex/, not in request/). No decision-branching or walking in this file.

    LIVE CONVEX PUSH (visible, mandatory — Phase 4 hit a Convex bundle blocker, keep this explicit): after the schema + adapters compile, run the project's Convex push/dev command to deploy `approvalChains`/`approvalTasks` + the `requesterId` column to the live deployment and regenerate `convex/_generated/`. Confirm the new tables + indexes exist and that the generated dataModel typechecks. If the Convex bundle/deploy fails (as in Phase 4), surface the exact error and STOP rather than marking complete.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test -- --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "approvalChains\|approvalTasks" convex/schema.ts` shows both tables; `grep -n "by_tenant_request_evaluation\|by_tenant_chain\|by_tenant_approver\|by_tenant_created" convex/schema.ts` shows all four indexes; `grep -n "requesterId" convex/schema.ts` shows the nullable column.
    - `grep -n "requesterId" src/modules/request/adapters/convex/convex-request-evaluation-repository.ts src/modules/request/adapters/convex/mappers.ts` matches in both (Convex ripple closed).
    - `grep -n "doc.tenantId !== fromTenantId(ctx.tenantId)\|tenantId !== fromTenantId" src/modules/approval/adapters/convex/convex-approval-chain-repository.ts src/modules/approval/adapters/convex/convex-approval-task-repository.ts` matches in both (CON-01).
    - LIVE ACTOR (BLOCKER 1b): `grep -n "actorId: v.string()" convex/request.ts` matches the mutation args; `grep -n "userId(args.actorId)" convex/request.ts` matches the tCtx build; `grep -n "tenantContext(tenantId(args.tenantId), userId(args.actorId))" convex/request.ts` matches.
    - REPO CONSTRUCTION (WARNING #3): `grep -n "new ConvexUserRepository(ctx.db)\|new ConvexRoleRepository(ctx.db)" convex/request.ts` matches both; `grep -n "new ConvexApprovalChainRepository(ctx.db)\|new ConvexApprovalTaskRepository(ctx.db)" convex/request.ts` matches both; `grep -n "new ApprovalRoutingService(" convex/request.ts` matches.
    - `grep -n "on(\"RequestEvaluated\"" convex/request.ts` matches and `grep -n "onRequestEvaluated(tCtx" convex/request.ts` matches (subscription wired with the actor-bearing ctx). No `decision.kind` / manager-walk in convex/request.ts (`! grep -n "kind ===\|managerId" convex/request.ts`).
    - `! grep -rn "modules/approval" src/modules/request` (SC#1 preserved).
    - LIVE PUSH: the Convex push/dev command completed; `convex/_generated/dataModel.d.ts` references `approvalChains` + `approvalTasks` (`grep -rn "approvalChains" convex/_generated`); typecheck green against generated types. On bundle failure, executor stopped and surfaced the error.
    - `npm test` full suite green; `npm run lint` + `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Convex schema has the two new tables + requesterId column live on the deployment with tenant-prefixed/reverse-lookup indexes; adapters persist with tenant scoping; submitRequest carries an actorId arg and constructs the user/role/chain/task repos so routing is subscribed to "RequestEvaluated" with a non-null live requester and no domain logic in convex/; SC#1 holds; full suite + lint + typecheck green.</done>
</task>

<task type="auto">
  <name>Task 4: Pin approval coverage to 100% and run the phase coverage gate</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (whole file — the thresholds block; the existing `src/modules/runtime/**/*.ts` 100% pin lines 23-28; the coverage exclude list line 17 already excludes index.ts + adapters/convex/**)
    - 05-VALIDATION.md "Coverage targets" + "Sampling Rate" (100% on domain+application+memory; engineering.md "Approval Workflow Generation: 100%")
    - docs/engineering.md lines 35-53 (the 100% mandate)
  </read_first>
  <action>
    vitest.config.ts: add a per-folder threshold entry `'src/modules/approval/**/*.ts': { lines: 100, functions: 100, branches: 100, statements: 100 }` mirroring the existing runtime pin. The global exclude (index.ts + adapters/convex/** + *.test.ts) already scopes this to domain + application + memory adapters — so the 100% applies exactly where the in-memory-fake tests reach. Then run the full coverage gate.
  </action>
  <verify>
    <automated>npm run test:coverage && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "modules/approval" vitest.config.ts` shows the 100% pin entry inside `thresholds`.
    - `npm run test:coverage` exits 0 with `src/modules/approval/**` (domain+application+memory) at 100% lines/functions/branches/statements and global ≥ 90%.
    - `npm run lint` passes (approval ESLint zone enforced) and `npx tsc --noEmit` exits 0.
    - SC#1 final check: `! grep -rn "modules/approval" src/modules/request`.
  </acceptance_criteria>
  <done>approval module is pinned and verified at 100% coverage (domain+application+memory); the full coverage + lint + typecheck phase gate is green, ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approver → act() | `ctx.actorId` (untrusted identity) must equal the stored `task.approverId` before any state change — the core authorization gate (D-52). |
| RequestEvaluated subscriber → submit() transaction | Routing runs inside the submit mutation/transaction; a routing failure must NOT roll back the persisted RequestEvaluation (D-54 / Open-Q2). |
| submitRequest actorId arg → requesterId | The caller-supplied `actorId` becomes the routing anchor (`requesterId`). It is untrusted identity; the walk + tenant guards bound what it can reach, and a null/absent requester is rejected by the null-requester guard (Task 1) before any directory read. |
| tenant data reads (chain/task/eval) | Every Convex + memory read filters by `ctx.tenantId` (CON-01, D-09 tenant-prefixed indexes). |
| Decision (runtime) → consumer | Routing reads decision ONLY from the persisted RequestEvaluation, never re-derives or trusts event payload content (D-47); the runtime stays agnostic (SC#1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Elevation of Privilege | ApprovalRoutingService.act() | mitigate | `ctx.actorId !== task.approverId → UnauthorizedApproverError` before any transition (D-52). Approver is a specific resolved user, not a role bucket. Tested (non-approver → error, no state change/event). |
| T-05-11 | Tampering (double-action / replay) | act() on a resolved task | mitigate | `task.state !== "PENDING" → TaskAlreadyResolvedError` (idempotency/terminal guard, D-52), checked BEFORE the auth guard. Tested. |
| T-05-12 | Tampering (duplicate routing) | onRequestEvaluated re-delivery | mitigate | `chainRepo.findByRequestEvaluationId` pre-check no-ops a second delivery → exactly one chain (Pitfall 5). Backed by the `by_tenant_request_evaluation` reverse index. Tested. |
| T-05-13 | Denial of Service / data integrity | routing failure rolling back submit | mitigate | Service SWALLOWS RoutingError/RoleNotFoundError/HierarchyTraversalError → emits ApprovalRoutingFailed, never rethrows (Open-Q2/A3); the WHOLE onRequestEvaluated body (incl. pre-walk repo reads) is inside one try/catch so no failure mode bubbles. 05-01's hardened EventDispatcher is the defense-in-depth second layer. RequestEvaluation stays persisted (D-54). Tested. |
| T-05-14 | Denial of Service | corrupted/cyclic hierarchy during walk | mitigate | MAX_HIERARCHY_DEPTH=50 cap → HierarchyTraversalError (D-50). Defensive backstop to write-time D-11 cycle prevention. Tested (>50 hops). |
| T-05-15 | Information Disclosure | cross-tenant chain/task/eval reads | mitigate | Memory fakes guard `tenantId !== ctx.tenantId → null`; Convex adapters use tenant-prefixed `withIndex` + `doc.tenantId !== fromTenantId(ctx.tenantId)` guard (CON-01, D-09). Tested (cross-tenant read → null). |
| T-05-16 | Information Disclosure | audit log leaking content | mitigate | approval.* audit payloads carry IDs/metadata ONLY (taskId/chainId/actorId/evaluationRecordId/reason), never request or decision content (D-37/D-53). Test asserts no content keys. |
| T-05-17 | Repudiation | unaudited approval/routing actions | mitigate | Every Approve/Reject/routing-failure emits an event consumed by ApprovalAuditSubscriber → immutable by-reference AuditLog (D-53/D-54). Tested for all three event types. |
| T-05-18 | Tampering | convex/ holding business logic | mitigate | convex/request.ts only wires DI + subscribes; no decision-branching/walk (Pitfall 6). Enforced by ESLint convex zones + grep acceptance (`! grep "kind ===\|managerId" convex/request.ts`). |
| T-05-19 | Spoofing | submitRequest actorId arg (caller asserts identity) | accept | Phase 5 trusts the caller-supplied `actorId` as the requester (no auth/session layer yet — deferred with the approver inbox to Phase 6). The null-requester guard rejects absent identity; tenant + walk guards bound reachability. Accepted because there is no PII exposure beyond tenant scope and the authenticated session boundary is a Phase 6 concern (CONTEXT defers approver/identity UI). |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | Phase 5 installs ZERO external packages (05-RESEARCH Package Legitimacy Audit). No install task → no slopsquatting surface → no human-verify gate. |
</threat_model>

<verification>
- `npm run test:coverage` green: `src/modules/approval/**` (domain+application+memory) at 100%; global ≥ 90%.
- `npm test` full suite green — routing service, audit subscriber, dispatcher isolation, actorId→requesterId threading, and all Phase 1-4 suites.
- `npm run lint` + `npx tsc --noEmit` clean (approval ESLint zone enforced; SC#1 `! grep -rn "modules/approval" src/modules/request`).
- LIVE Convex deployment carries `approvalChains` + `approvalTasks` + `requestEvaluations.requesterId`; `convex/_generated` reflects them and typechecks; `submitRequest` accepts the `actorId` arg.
- GitNexus: `gitnexus_impact` run before editing the convex/request.ts wiring symbol; `gitnexus_detect_changes()` run pre-commit confirms only expected files/symbols changed.
</verification>

<success_criteria>
- SC#1: ApprovalRoutingService subscribes to RequestEvaluated; runtime + request/ have zero compile-time dependency on approval/.
- SC#2: manager `managerId` chain is dynamically walked (self excluded) to the first ancestor holding the role.
- SC#3: target role is validated against the Role registry (RoleNotFoundError when absent) before routing.
- SC#4: sequential multi-stage is supported at the entity/state-machine level (stageNumber + terminal transitions); Phase 5 execution is single-task (stageNumber=1) — multi-stage data deliberately deferred, not a gap.
- SC#5: Approve/Reject drive deterministic task + chain state transitions with auth + idempotency guards (service + tests this phase; the live approveTask/rejectTask mutation is deliberately deferred to Phase 6 with the inbox UI — stated, not omitted).
- DEC-03 delivered end-to-end: the live submitRequest threads a non-null actor → requesterId → routing materializes a chain; routing failures isolated from submission (D-54); all actions audited by-reference (D-53).
- Live Convex schema push verified; approval module at 100% coverage; lint + typecheck + full suite green.
</success_criteria>

<output>
Create `.planning/phases/05-approval-routing/05-02-SUMMARY.md` when done.
</output>
