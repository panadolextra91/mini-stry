---
phase: 06-admin-portal-ui-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - convex/policy.ts
  - convex/audit.ts
  - convex/approval.ts
  - convex/request.ts
  - convex/seed.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]
user_setup: []
must_haves:
  truths:
    - "The UI can list policies and versions for a tenant via convex/policy.ts queries (D-61)"
    - "The UI can save-draft, publish, and rollback policies via thin handlers that call PolicyService (D-61)"
    - "The UI can list an approver's inbox and approve/reject tasks via convex/approval.ts (D-61)"
    - "The UI can list audit logs and requests for a tenant (D-61)"
    - "A repeatable, idempotent seed populates the full demo dataset for the cross-user flow (D-62)"
  artifacts:
    - path: "convex/policy.ts"
      provides: "listPolicies, listVersions, saveDraft, publish, rollback thin handlers"
      contains: "listPolicies"
    - path: "convex/approval.ts"
      provides: "listInbox, approve, reject thin handlers"
      contains: "listInbox"
    - path: "convex/audit.ts"
      provides: "listAuditLogs thin handler"
      contains: "listAuditLogs"
    - path: "convex/seed.ts"
      provides: "idempotent seedDemoData internalMutation"
      contains: "internalMutation"
  key_links:
    - from: "convex/policy.ts"
      to: "PolicyService + ConvexPolicyRepository"
      via: "thin DI assembly, no domain logic"
      pattern: "new PolicyService"
    - from: "convex/approval.ts"
      to: "ApprovalRoutingService.act with actorId"
      via: "tenantContext(tenantId, userId)"
      pattern: "routingService.act"
---

<objective>
Fill the empty/absent Convex handlers the UI calls, and add an idempotent demo-data seed. `convex/policy.ts` and `convex/audit.ts` are `export {}` stubs today; `convex/approval.ts` does not exist; `convex/request.ts` needs a `listRequests` query. Every handler is a thin DI adapter (HARD RULE, D-61) — parse args, build TenantContext, assemble dependencies, call the existing application service, return. No business logic, evaluation, routing, or lifecycle logic enters `convex/`.

Purpose: Expose the Phase 1-5 application services + the 06-01 read paths over the network so the React client can consume them via reactive useQuery/useMutation. The seed (D-62) populates the deterministic demo dataset that the cross-user reactive flow (D-63) showcases.
Output: 5 handler files filled/created + 1 seed file. After this plan, `convex dev` regenerates `convex/_generated/api` with all UI endpoints.
</objective>

<execution_context>
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-admin-portal-ui-dashboard/06-PATTERNS.md
@.planning/phases/06-admin-portal-ui-dashboard/06-01-SUMMARY.md

<interfaces>
<!-- Verified handler template + service signatures. Mirror convex/request.ts exactly. -->

HARD-RULE doc-comment header (copy verbatim to top of every handler file): the 5-line block at convex/request.ts lines 1-5.

Imports: query/mutation/internalMutation from "./_generated/server.js"; v from "convex/values"; services + Convex adapters from "../src/modules/<mod>/index.js" (barrel) or deep "../src/modules/<mod>/adapters/convex/...js" (allowed for DI only); tenantContext, tenantId, userId from "../src/modules/directory/index.js". ALL relative imports use .js extensions (ESM).

Read handler shape (mirror getRequestEvaluation, convex/request.ts lines 95-105):
  query({ args: { tenantId: v.string(), ... }, handler: (ctx, args) => { const repo = new Convex...Repository(ctx.db); const tCtx = tenantContext(tenantId(args.tenantId)); return repo.<method>(tCtx, ...); } })

PolicyService DI graph (convex/request.ts lines 45-54): new EventDispatcher<PolicyEventMap>(), ConvexPolicyRepository, ConvexPolicyVersionRepository, AjvSchemaValidator, then new PolicyService(policyRepo, versionRepo, policyValidator, policyDispatcher).
Verified PolicyService signatures (src/modules/policy/application/policy-service.ts):
  - saveDraft(ctx, versionId, content, expectedRevision)  [optimistic concurrency arg]
  - publishDraft(ctx, versionId)  [server Ajv gate lives HERE, D-58 — not in handler]
  - rollback(ctx, policyId, targetVersionId, actorUserId)  [creates forward clone; never mutates history, D-60]
  - createPolicy / createDraft also exist (needed by seed and/or a create-draft handler).

Approval DI graph (convex/request.ts lines 65-80): ConvexUserRepository, ConvexRoleRepository, ConvexApprovalChainRepository, ConvexApprovalTaskRepository, ConvexAuditLogRepository, ConvexRequestEvaluationRepository, EventDispatcher<ApprovalEventMap>, void new ApprovalAuditSubscriber(auditRepo, approvalDispatcher), then new ApprovalRoutingService(chainRepo, taskRepo, userRepo, roleRepo, evalRepo, approvalDispatcher).
Verified act signature: act(ctx, taskId, action: "APPROVE" | "REJECT"): Promise<void>. ctx MUST carry actorId or act throws UnauthorizedApproverError.

Existing reusable methods (added in 06-01 or already present):
  - ConvexPolicyRepository.listByTenant(ctx)  [06-01]
  - ConvexPolicyVersionRepository.listByPolicy(ctx, policyId)  [06-01]
  - ConvexApprovalTaskRepository.findByApprover(ctx, approverId)  [06-01]
  - ConvexAuditLogRepository.findByTenant(ctx)  [already exists]
  - ConvexRequestEvaluationRepository.findByTenant(ctx)  [already exists]

Branded ID factories: policyId, policyVersionId (policy barrel); userId (directory barrel); approvalTaskId (approval barrel); requestEvaluationId (request barrel).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill convex/policy.ts with policy list + lifecycle handlers and add listRequests to convex/request.ts</name>
  <files>convex/policy.ts, convex/request.ts</files>
  <read_first>
    - convex/policy.ts (file being modified; currently export {} stub)
    - convex/request.ts (file being modified for listRequests; also the verified DI/handler template to mirror for policy)
    - convex/directory.ts (multi-handler file shape + getRole thin query template)
    - src/modules/policy/application/policy-service.ts (verify saveDraft/publishDraft/rollback/createDraft signatures before wiring)
    - src/modules/policy/index.ts (PolicyService + adapter + ID factory exports)
    - .planning/phases/06-admin-portal-ui-dashboard/06-PATTERNS.md (sections "convex/policy.ts" and "convex/request.ts — add listRequests")
  </read_first>
  <action>Replace convex/policy.ts export {} with the HARD-RULE header then thin handlers: listPolicies query (args tenantId: v.string()) → new ConvexPolicyRepository(ctx.db); return repo.listByTenant(tenantContext(tenantId(args.tenantId))); listVersions query (args tenantId, policyId: v.string()) → new ConvexPolicyVersionRepository(ctx.db); return repo.listByPolicy(tCtx, policyId(args.policyId)); saveDraft mutation (args tenantId, versionId, content: v.any(), expectedRevision: v.number()) → assemble the PolicyService DI graph exactly as convex/request.ts lines 45-54 and call policyService.saveDraft(tCtx, policyVersionId(args.versionId), args.content, args.expectedRevision); publish mutation (args tenantId, versionId) → call policyService.publishDraft(tCtx, policyVersionId(args.versionId)) — the Ajv gate is inside the service, NOT here; rollback mutation (args tenantId, policyId, targetVersionId, actorId) → build tenantContext(tenantId(args.tenantId), userId(args.actorId)) and call policyService.rollback(tCtx, policyId(args.policyId), policyVersionId(args.targetVersionId), userId(args.actorId)). Also add a createDraft mutation if the editor needs to start a new draft (call policyService.createDraft / createPolicy per verified signatures). Attach the audit subscriber to the policyDispatcher for lifecycle events if PolicyEventMap is consumed by an audit subscriber (confirm which subscriber consumes PolicyEventMap; mirror the void new ...Subscriber side-effect from convex/request.ts lines 41-43). In convex/request.ts add a listRequests query (args tenantId) → new ConvexRequestEvaluationRepository(ctx.db); return evalRepo.findByTenant(tenantContext(tenantId(args.tenantId))). NO filtering/branching/transform in any handler body.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export {}" convex/policy.ts` returns 0 (stub removed).
    - `grep -n "listPolicies\|listVersions\|saveDraft\|publish\|rollback" convex/policy.ts` shows all five handler exports.
    - `grep -n "listRequests" convex/request.ts` shows the new query.
    - No business branch in handlers: `grep -vn '^\s*\*\|^\s*//' convex/policy.ts | grep -cE '\bif\s*\(' ` returns 0 (no if-branches in handler logic).
    - `npm run lint` passes (import/no-restricted-paths zones intact); `npm run typecheck` clean.
  </acceptance_criteria>
  <done>convex/policy.ts has 5 thin lifecycle/list handlers; convex/request.ts has listRequests; lint + typecheck clean; no domain logic in handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Create convex/approval.ts and fill convex/audit.ts thin handlers</name>
  <files>convex/approval.ts, convex/audit.ts</files>
  <read_first>
    - convex/audit.ts (file being modified; currently export {} stub)
    - convex/request.ts (submitRequest lines 32-93 = the full approval DI graph to copy; getRequestEvaluation = thin read template)
    - src/modules/approval/application/approval-routing-service.ts (verify act() signature + UnauthorizedApproverError guard)
    - src/modules/approval/index.ts (ApprovalRoutingService, ApprovalAuditSubscriber, adapters, approvalTaskId exports)
    - src/modules/audit/index.ts (ConvexAuditLogRepository export; findByTenant already exists)
    - .planning/phases/06-admin-portal-ui-dashboard/06-PATTERNS.md (sections "convex/approval.ts" and "convex/audit.ts")
  </read_first>
  <action>Create convex/approval.ts with the HARD-RULE header then: listInbox query (args tenantId, actorId: v.string()) → new ConvexApprovalTaskRepository(ctx.db); return repo.findByApprover(tenantContext(tenantId(args.tenantId)), userId(args.actorId)); approve mutation and reject mutation (args tenantId, actorId, taskId: v.string()) → assemble the approval DI graph exactly as convex/request.ts lines 65-80 (userRepo, roleRepo, chainRepo, taskRepo, auditRepo, evalRepo, EventDispatcher<ApprovalEventMap>, void new ApprovalAuditSubscriber(auditRepo, approvalDispatcher), new ApprovalRoutingService(...)), build tCtx = tenantContext(tenantId(args.tenantId), userId(args.actorId)) (BOTH args — Pitfall 5), and call routingService.act(tCtx, approvalTaskId(args.taskId), "APPROVE") / "REJECT". Replace convex/audit.ts export {} with the HARD-RULE header then listAuditLogs query (args tenantId) → new ConvexAuditLogRepository(ctx.db); return repo.findByTenant(tenantContext(tenantId(args.tenantId))). No filtering/branching in any handler.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "listInbox\|export const approve\|export const reject" convex/approval.ts` shows all three handlers.
    - `grep -n "tenantContext(tenantId(args.tenantId), userId(args.actorId))" convex/approval.ts` confirms actorId is passed to approve/reject (Pitfall 5).
    - `grep -c "export {}" convex/audit.ts` returns 0; `grep -n "listAuditLogs" convex/audit.ts` shows the handler.
    - `npm run lint` passes; `npm run typecheck` clean.
  </acceptance_criteria>
  <done>convex/approval.ts has listInbox/approve/reject passing actorId; convex/audit.ts has listAuditLogs; lint + typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Create idempotent demo-data seed (convex/seed.ts)</name>
  <files>convex/seed.ts</files>
  <read_first>
    - convex/seed.ts (new file)
    - convex/schema.ts (all 9 tables + tenant-prefixed indexes the seed writes to)
    - convex/request.ts (submitRequest — to optionally drive a request→evaluation→approval chain so the demo has live tasks)
    - src/modules/directory/adapters/convex/convex-tenant-repository.ts (db.insert(...).get(...) raw write shape)
    - .planning/phases/06-admin-portal-ui-dashboard/06-PATTERNS.md (section "convex/seed.ts")
    - .planning/phases/06-admin-portal-ui-dashboard/06-RESEARCH.md (Pattern 6 idempotent seed)
  </read_first>
  <action>Create convex/seed.ts exporting seedDemoData as an internalMutation (NOT public — bootstrap concern, D-62). Begin with the idempotency guard: const existing = await ctx.db.query("tenants").first(); if (existing) return { seeded: false }. Then insert the full graph in dependency order: 2 tenants → roles per tenant → users with a managerId reporting hierarchy (so manager-walk resolution produces multi-stage chains) → policies → policyVersions (at least one published+active per tenant with valid PolicyContent that passes the canonical schema) → at least one requestEvaluation that yields a Request-Approval Decision → the resulting approvalChain + approvalTasks (assigned to a seeded approver) → auditLogs. The seed may insert directly via ctx.db for plain entities, but for entities with invariants (users + manager hierarchy, policy versions) prefer reusing the Convex repositories/services for correctness. Ensure at least one task is PENDING and assigned to a known demo approver so the Inbox is non-empty and the D-63 cross-user flow (requester submits → approver inbox updates → approver acts → audit updates) is demonstrable. Return { seeded: true } on first run. Run via `npx convex run seed:seedDemoData`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "internalMutation" convex/seed.ts` confirms seedDemoData is an internalMutation (not public query/mutation).
    - `grep -n "first()" convex/seed.ts` shows the idempotency guard.
    - `npm run typecheck` clean.
    - Manual (recorded in SUMMARY): `npx convex run seed:seedDemoData` returns { seeded: true } first run and { seeded: false } on a second run with no duplicate tenants.
  </acceptance_criteria>
  <done>seedDemoData internalMutation populates the full demo graph idempotently; typecheck clean; manual double-run shows no duplicates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → convex handler | Untrusted args (tenantId, actorId, taskId, content) cross here; no auth this phase (D-55) |
| handler → application service | TenantContext is constructed here and passed as first arg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering / Input | all handler args | mitigate | Validate every arg with v.* validators; content is v.any() and only reaches Ajv server-side on publish (authoritative, D-58) |
| T-06-05 | Spoofing / Elevation | approve/reject handlers | mitigate | act() enforces ctx.actorId === task.approverId (UnauthorizedApproverError); handlers pass real actorId via tenantContext(tenantId, userId) |
| T-06-06 | Information Disclosure | every list query | mitigate | Reads delegate to 06-01 tenant-scoped adapter methods; no cross-tenant filtering in the handler |
| T-06-07 | Information Disclosure (deferred) | client-supplied tenantId/actorId | accept | D-55 defers auth: identity is client-supplied and unauthenticated. Isolation is enforced (queries scoped) but not authenticated. Documented, accepted demo-scope risk; full ownership verification ships with auth in a future phase |
| T-06-SC | Tampering | npm/convex installs | accept | This plan adds no new packages (uses existing convex builders); package audit in 06-03 covers frontend installs |
</threat_model>

<verification>
- `npm run lint` clean — import/no-restricted-paths zones prove handlers only import services/contexts via barrels (deep convex adapter imports allowed for DI only).
- `npm run typecheck` clean.
- `npm test` still green (handlers are not unit-tested but must not break the suite/typegen).
- Manual: `npx convex run seed:seedDemoData` idempotency double-run check recorded in SUMMARY.
- After commit, `convex dev` regenerates convex/_generated/api with the new endpoints (prerequisite for 06-03+ frontend).
</verification>

<success_criteria>
- convex/policy.ts, convex/approval.ts, convex/audit.ts filled; convex/request.ts has listRequests; convex/seed.ts created.
- All handlers are thin DI assembly (no domain logic) — verified by lint zones + no-if grep.
- approve/reject pass actorId; publish defers to PolicyService Ajv gate.
- Seed is idempotent.
</success_criteria>

<output>
Create `.planning/phases/06-admin-portal-ui-dashboard/06-02-SUMMARY.md` when done.
</output>
