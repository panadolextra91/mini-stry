---
phase: 06-admin-portal-ui-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - web/package.json
  - web/vite.config.ts
  - web/tsconfig.json
  - web/components.json
  - web/index.html
  - web/.env.local
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/index.css
  - web/src/lib/utils.ts
  - web/src/lib/convex.ts
  - web/src/app/context/DemoContext.tsx
  - web/src/components/AppShell.tsx
  - web/src/components/DemoContextSelector.tsx
  - web/src/routes/PolicyPortal.tsx
  - web/src/routes/RequestCenter.tsx
  - web/src/routes/Inbox.tsx
  - web/src/routes/Governance.tsx
  - web/src/components/ui/
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]
user_setup:
  - service: convex
    why: "Frontend connects to the existing Convex deployment for reactive queries"
    env_vars:
      - name: VITE_CONVEX_URL
        source: "Existing Convex deployment URL (Convex dashboard → Settings → Deployment URL, or the URL convex dev prints). Public, not a secret."
must_haves:
  truths:
    - "A dark-only React SPA runs under web/ and connects to the existing Convex deployment via VITE_CONVEX_URL (D-V3, D-63)"
    - "The Demo Context Selector lets the user pick an Active Tenant + Active User, producing {tenantId, actorId} (D-55)"
    - "Switching the tenant reloads all views scoped to the new tenant (D-56)"
    - "A persistent left nav rail exposes all 4 modules with no role gating (D-57)"
    - "shadcn is initialized New York/Zinc/CSS-vars/dark with the locked HSL token contract"
  artifacts:
    - path: "web/src/main.tsx"
      provides: "ConvexProvider + ConvexReactClient(VITE_CONVEX_URL)"
      contains: "ConvexProvider"
    - path: "web/src/app/context/DemoContext.tsx"
      provides: "DemoContext { tenantId, actorId } + useDemoContext hook"
      contains: "useDemoContext"
    - path: "web/src/components/AppShell.tsx"
      provides: "Left nav rail + 4 module routes (D-57)"
      contains: "nav"
    - path: "web/src/index.css"
      provides: ".dark HSL token contract from UI-SPEC"
      contains: ".dark"
  key_links:
    - from: "web/src/components/DemoContextSelector.tsx"
      to: "DemoContext state"
      via: "setTenant/setActor updates context consumed by every useQuery"
      pattern: "useDemoContext"
    - from: "web/src/main.tsx"
      to: "existing Convex deployment"
      via: "ConvexReactClient(import.meta.env.VITE_CONVEX_URL)"
      pattern: "VITE_CONVEX_URL"
---

<objective>
Scaffold the greenfield React SPA under web/ without colliding with the existing backend (root @/* → src/*, existing convex/), wire it to the existing Convex deployment, initialize shadcn/ui with the locked dark-mode HSL contract, and build the global shell: persistent left nav rail (4 modules, no role gating), the Demo Context Selector that produces {tenantId, actorId}, and the 4 placeholder routes. This is the integration spine every feature plan (06-04, 06-05) builds on.

Purpose: Establish the frontend foundation, the Demo Context Selector identity mechanism (D-55), and the dark visual system (UI-SPEC) so subsequent plans only add feature panes. The shell proves tenant switching reloads views (D-56) before any feature is built.
Output: web/ Vite app, shadcn primitives, dark theme, AppShell + DemoContext + DemoContextSelector + 4 routed placeholder pages.
</objective>

<execution_context>
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-admin-portal-ui-dashboard/06-UI-SPEC.md
@.planning/phases/06-admin-portal-ui-dashboard/06-RESEARCH.md
@.planning/phases/06-admin-portal-ui-dashboard/06-02-SUMMARY.md

<interfaces>
<!-- Greenfield: no in-repo analog. Use RESEARCH patterns + UI-SPEC contract. -->

RESEARCH Pattern 1 (scaffold without collision): web/ separate Vite root; own web/tsconfig.json with paths @/* → ./src/*; import the generated Convex API from the repo-root convex/_generated/api (relative ../../convex/_generated/api or a dedicated alias). Do NOT re-run convex init — the deployment already exists.

RESEARCH main.tsx (verified): new ConvexReactClient(import.meta.env.VITE_CONVEX_URL as string) inside <ConvexProvider client={convex}>.

RESEARCH Pattern 2 (Demo Context): React context holds { tenantId, actorId }; every useQuery/useMutation passes them as args; handlers turn them into TenantContext server-side (mirrors convex/request.ts submitRequest args tenantId: v.string(), actorId: v.string()).

UI-SPEC color contract (set under .dark in index.css, HSL triples): background 240 10% 4%; card/secondary 240 6% 9%; border 240 5% 16%; foreground 0 0% 98%; muted-foreground 240 5% 60%; accent/primary 217 91% 60%; destructive 0 72% 51%. Fonts: Geist Sans (UI), Geist Mono (code/IDs); fallbacks Inter/system-ui and ui-monospace.

UI-SPEC shell contract: persistent left nav rail (secondary surface) listing all 4 modules always visible (D-57); Demo Context Selector pinned top of the rail; selection drives TenantContext; switching tenant reloads all views (D-56). Accent reserved (10% budget) — nav active selection is one of the allowed accent uses.

Endpoints available after 06-02 (in convex/_generated/api): api.policy.{listPolicies,listVersions,saveDraft,publish,rollback}, api.approval.{listInbox,approve,reject}, api.audit.listAuditLogs, api.request.{submitRequest,getRequestEvaluation,listRequests}. Directory queries exist for the tenant/user selector data source (read tenants + users-by-tenant; if a list-tenants / list-users-by-tenant query is missing, add a thin one in convex/directory.ts mirroring the existing thin handlers — query mechanics stay in the adapter).

Routing: react-router-dom 7.16.0 — 4 routes: /policies (PolicyPortal), /requests (RequestCenter), /inbox (Inbox), /governance (Governance).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate before frontend installs</name>
  <what-built>Nothing yet — this gate runs BEFORE any npm install. The frontend introduces new packages not yet in the repo.</what-built>
  <read_first>
    - .planning/phases/06-admin-portal-ui-dashboard/06-RESEARCH.md (Package Legitimacy Audit table — all packages Approved via npm view; slopcheck SLOP verdicts were PyPI ecosystem mismatches)
  </read_first>
  <how-to-verify>
    The RESEARCH Package Legitimacy Audit lists every package as Approved (npm-verified, zero postinstall scripts). slopcheck only supports PyPI, so its [SLOP] verdicts for these npm packages are ecosystem mismatches, not hallucination signals. Confirm the following packages are the intended, legitimate npm packages before install (spot-check on npmjs.com if desired):
    1. react / react-dom 19.2.7, vite 8.0.16, @vitejs/plugin-react 6.0.2
    2. convex (PIN to the backend's version 1.39.1 — Pitfall 6, do not bump to 1.40.0)
    3. tailwindcss + @tailwindcss/vite 4.3.0, react-router-dom 7.16.0
    4. @monaco-editor/react 4.7.0 + monaco-editor 0.55.1
    5. lucide-react 1.17.0 (1.x is current latest, not a typo), class-variance-authority 0.7.1, clsx 2.1.1, tailwind-merge 3.6.0, sonner 2.0.7
    Note lucide-react 1.17.0 specifically — verify it is the lucide-icons/lucide package on npmjs.com/package/lucide-react.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the package list is legitimate (or flags any package to re-verify).
    - convex is pinned to 1.39.1, matching the backend.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with installs, or name any package to re-verify.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold web/ Vite app, init shadcn, apply dark HSL theme, wire ConvexProvider</name>
  <files>web/package.json, web/vite.config.ts, web/tsconfig.json, web/components.json, web/index.html, web/.env.local, web/src/main.tsx, web/src/App.tsx, web/src/index.css, web/src/lib/utils.ts, web/src/lib/convex.ts, web/src/components/ui/</files>
  <read_first>
    - .planning/phases/06-admin-portal-ui-dashboard/06-RESEARCH.md (Installation block, Recommended Project Structure, Pattern 1, Pitfall 2 alias collision, Pitfall 7 Tailwind v4)
    - .planning/phases/06-admin-portal-ui-dashboard/06-UI-SPEC.md (Color, Typography, Spacing, Design System — the authoritative visual contract)
    - tsconfig.json (root — confirm @/* → src/* so web/ uses a SEPARATE tsconfig to avoid collision)
    - convex/_generated/api.d.ts (confirm the generated api exports include the 06-02 endpoints; ensure convex dev has run)
  </read_first>
  <action>Scaffold under web/ with `npm create vite@latest web -- --template react-ts` (Claude's discretion: web/ avoids colliding with src/ and convex/). Install frontend deps per RESEARCH Installation block, PINNING convex to 1.39.1 (Pitfall 6). Install dev deps tailwindcss + @tailwindcss/vite + @types/react + @types/react-dom. Configure web/vite.config.ts with @vitejs/plugin-react + @tailwindcss/vite + alias @/* → ./src/* (scoped to web/src; Pitfall 2). Give web/ its own tsconfig.json with paths @/* → ./src/* and a path/alias or relative import for ../../convex/_generated/api. Run `npx shadcn@latest init` (New York style, base color Zinc, CSS variables yes, dark default — UI-SPEC) then `npx shadcn@latest add button card dialog select table badge input textarea tabs separator scroll-area tooltip dropdown-menu skeleton sonner`. In web/src/index.css use `@import "tailwindcss";` (Tailwind v4, Pitfall 7) and define the .dark HSL token block from UI-SPEC exactly (background/card/border/foreground/muted-foreground/primary/destructive triples); apply the .dark class at root and expose NO theme toggle (D-V3). Set Geist Sans / Geist Mono font stacks with the UI-SPEC fallbacks. In web/src/main.tsx wrap <App/> in <ConvexProvider client={new ConvexReactClient(import.meta.env.VITE_CONVEX_URL)}> (RESEARCH main.tsx) and mount <Toaster/> from sonner. Put the ConvexReactClient construction in web/src/lib/convex.ts; cn() helper in web/src/lib/utils.ts (shadcn default). Create web/.env.local with VITE_CONVEX_URL (do NOT commit it; add to web/.gitignore). web/src/App.tsx renders a minimal dark page proving Tailwind + the dark tokens apply (full shell comes in Task 3).</action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` succeeds (Tailwind v4 + alias resolve; no @/* collision with backend).
    - `cd web && npx tsc --noEmit` clean.
    - `grep -n "ConvexProvider" web/src/main.tsx` present.
    - `grep -n "240 10% 4%" web/src/index.css` confirms the UI-SPEC background token is applied under .dark.
    - `grep -n "VITE_CONVEX_URL" web/.gitignore` OR `.env.local` listed in web/.gitignore (secret/url not committed).
    - web/components.json exists with style New York and baseColor zinc.
  </acceptance_criteria>
  <done>web/ builds; shadcn initialized New York/Zinc/dark; .dark HSL contract applied; ConvexProvider wired to VITE_CONVEX_URL; no alias collision.</done>
</task>

<task type="auto">
  <name>Task 3: Build AppShell, DemoContext + DemoContextSelector, and 4 routed placeholder pages</name>
  <files>web/src/app/context/DemoContext.tsx, web/src/components/AppShell.tsx, web/src/components/DemoContextSelector.tsx, web/src/routes/PolicyPortal.tsx, web/src/routes/RequestCenter.tsx, web/src/routes/Inbox.tsx, web/src/routes/Governance.tsx, web/src/App.tsx</files>
  <read_first>
    - .planning/phases/06-admin-portal-ui-dashboard/06-UI-SPEC.md (Interaction & Layout Contract → Global shell row; Color accent-reserved list; Copywriting empty-state strings)
    - .planning/phases/06-admin-portal-ui-dashboard/06-RESEARCH.md (Pattern 2 Demo Context, Pattern 5 reactive useQuery)
    - .planning/phases/06-admin-portal-ui-dashboard/06-CONTEXT.md (D-55/56/57 identity + scoping rules)
    - convex/directory.ts (existing directory handlers — source of tenants + users for the selector; add thin list handlers here if missing)
  </read_first>
  <action>Create web/src/app/context/DemoContext.tsx: a React context holding { tenantId, actorId } plus setters and a useDemoContext() hook (RESEARCH Pattern 2). The selected actorId list is filtered by the selected tenantId (D-56). Create web/src/components/DemoContextSelector.tsx using shadcn Select: an Active Tenant select (data from a tenants query) and an Active User select filtered to the selected tenant (data from a users-by-tenant query); changing the tenant clears/repicks the user and triggers all dependent views to reload (D-56). If convex/directory.ts lacks list-tenants or list-users-by-tenant thin queries, add them there (thin DI only — query mechanics in the directory adapter; mirror existing thin handlers). Create web/src/components/AppShell.tsx: a persistent left nav rail (secondary surface 240 6% 9%) with the DemoContextSelector pinned at top and 4 NavLinks (Policy Portal, Request Center, Personal Inbox, Governance) — all always visible, no role gating (D-57); the active route uses the reserved accent (217 91% 60%) per UI-SPEC. Set up react-router-dom routes in App.tsx: /policies, /requests, /inbox, /governance, each rendering the corresponding route component inside AppShell; default redirect to /policies. Create the 4 route placeholder components (PolicyPortal, RequestCenter, Inbox, Governance) rendering the module title (Display 28px/600) and the UI-SPEC empty-state copy for that module as a placeholder body — feature panes are added in 06-04/06-05.</action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` succeeds.
    - `grep -n "useDemoContext" web/src/app/context/DemoContext.tsx` present.
    - `grep -cn "NavLink\|Link" web/src/components/AppShell.tsx` shows 4 module links.
    - `grep -n "No tasks waiting on you" web/src/routes/Inbox.tsx` confirms the UI-SPEC empty-state copy is used.
    - The 4 routes (/policies, /requests, /inbox, /governance) are registered in App.tsx.
  </acceptance_criteria>
  <done>AppShell renders 4-module nav + Demo Context Selector; DemoContext provides {tenantId, actorId}; 4 routes render placeholders with UI-SPEC copy; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the dark shell + tenant switch reactivity</name>
  <what-built>The web/ SPA shell: dark theme, left nav rail with 4 modules, Demo Context Selector (Active Tenant + Active User), and 4 routed placeholder pages. The backend seed (06-02) populates the demo tenants/users the selector lists.</what-built>
  <how-to-verify>
    1. Ensure `npx convex run seed:seedDemoData` has been run (returns { seeded: true } once).
    2. From web/, run `npm run dev` and open the printed localhost URL.
    3. Confirm the UI is dark (deep near-black background, near-white text) — no light theme, no theme toggle (D-V3).
    4. The left nav rail shows all 4 modules; the active one is accent-highlighted (D-57).
    5. The Demo Context Selector lists the seeded tenants; selecting a tenant filters the Active User select to that tenant's users (D-56).
    6. Switch the Active Tenant — confirm the user list repopulates for the new tenant (proves selection drives state; feature views reload in 06-04/05).
    7. Visually spot-check spacing/typography/accent against 06-UI-SPEC.md (generous spacing, sharp type, single restrained accent).
  </how-to-verify>
  <acceptance_criteria>
    - Dark-only theme renders correctly; no light mode.
    - All 4 modules visible in the nav; active route accent-highlighted.
    - Tenant switch repopulates the user selector (D-56 reactivity baseline).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser SPA → Convex deployment | Client-supplied {tenantId, actorId} via Demo Context Selector; no auth (D-55) |
| build bundle → public | Only VITE_CONVEX_URL is exposed to the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure | web/ env / bundle | mitigate | Only VITE_CONVEX_URL (public deployment URL) is exposed; .env.local gitignored; never embed deploy/admin keys in web/ |
| T-06-09 | Spoofing (deferred) | Demo Context Selector identity | accept | D-55 intentionally has NO auth: tenant/actor are client-chosen for demo. Documented accepted risk; auth deferred to a future phase. Server-side tenant scoping (06-01/02) still enforced |
| T-06-SC | Tampering | npm install (frontend deps) | mitigate | Package legitimacy gate (Task 1) + RESEARCH audit: all npm-verified, zero postinstall; convex pinned to backend version |
</threat_model>

<verification>
- `cd web && npx vite build` and `npx tsc --noEmit` clean.
- `npm run lint` at repo root still clean (web/ is a separate root; backend lint zones unaffected).
- Manual checkpoint (Task 4): dark shell + tenant-switch reactivity verified against UI-SPEC.
</verification>

<success_criteria>
- web/ SPA builds and connects to the existing Convex deployment via VITE_CONVEX_URL.
- shadcn New York/Zinc/dark with the UI-SPEC HSL contract.
- Demo Context Selector produces {tenantId, actorId}; tenant switch repopulates users (D-55/56).
- 4-module nav, no role gating (D-57); 4 routes render placeholders.
</success_criteria>

<output>
Create `.planning/phases/06-admin-portal-ui-dashboard/06-03-SUMMARY.md` when done.
</output>
