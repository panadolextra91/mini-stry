---
phase: 06-admin-portal-ui-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/runtime/index.ts
  - src/modules/policy/ports/policy-repository.port.ts
  - src/modules/policy/ports/policy-version-repository.port.ts
  - src/modules/policy/adapters/convex/convex-policy-repository.ts
  - src/modules/policy/adapters/convex/convex-policy-version-repository.ts
  - src/modules/approval/ports/approval-task-repository.port.ts
  - src/modules/approval/adapters/convex/convex-approval-task-repository.ts
  - src/modules/approval/adapters/memory/in-memory-approval-task-repository.ts
  - tests/modules/runtime/schema-parity.test.ts
  - tests/modules/policy/convex-policy-repository.test.ts
  - tests/modules/policy/convex-policy-version-repository.test.ts
  - tests/modules/approval/convex-approval-task-repository.test.ts
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - "The canonical policy-content.schema.json is importable from the runtime barrel (D-59)"
    - "A tenant's policies can be listed via PolicyRepositoryPort.listByTenant, tenant-isolated"
    - "A policy's versions can be listed via PolicyVersionRepositoryPort.listByPolicy, tenant+policy-scoped"
    - "An approver's pending tasks can be listed via ApprovalTaskRepositoryPort.findByApprover, tenant+actor-scoped"
    - "The barrel-exported schema is the same artifact the Ajv runtime validator loads (parity invariant)"
  artifacts:
    - path: "src/modules/runtime/index.ts"
      provides: "policyContentSchema export (D-59)"
      contains: "policyContentSchema"
    - path: "src/modules/policy/ports/policy-repository.port.ts"
      provides: "listByTenant port method"
      contains: "listByTenant"
    - path: "src/modules/policy/ports/policy-version-repository.port.ts"
      provides: "listByPolicy port method"
      contains: "listByPolicy"
    - path: "src/modules/approval/ports/approval-task-repository.port.ts"
      provides: "findByApprover port method"
      contains: "findByApprover"
    - path: "tests/modules/runtime/schema-parity.test.ts"
      provides: "D-59 parity invariant test"
  key_links:
    - from: "src/modules/policy/adapters/convex/convex-policy-repository.ts"
      to: "policies table by_tenant_name index"
      via: "withIndex tenant-scoped query"
      pattern: "withIndex.*by_tenant_name"
    - from: "src/modules/approval/adapters/convex/convex-approval-task-repository.ts"
      to: "approvalTasks table by_tenant_approver index"
      via: "withIndex tenant+approver-scoped query"
      pattern: "withIndex.*by_tenant_approver"
---

<objective>
Add the genuinely-new backend read paths the four UI modules consume, and expose the canonical JSON Schema through the runtime barrel. The repositories built in Phases 1-5 cover write/evaluate paths but lack list-by-tenant capabilities; the UI cannot render policies, version histories, or inboxes until these exist. This plan also adds the highest-value Wave 0 test (schema parity, D-59) and tenant-isolation tests for every new read path.

Purpose: Unblock all UI plans (06-02..05). Every list adapter method here is exact-analog work mirroring an existing method in the same file. No new domain logic — pure read-path exposure.
Output: 3 new port methods (+Convex adapters, +memory fakes where they exist), 1 runtime barrel export, 4 test additions.
</objective>

<execution_context>
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-admin-portal-ui-dashboard/06-PATTERNS.md
@.planning/phases/06-admin-portal-ui-dashboard/06-VALIDATION.md

<interfaces>
<!-- Existing signatures the new methods mirror. Use directly; no exploration needed. -->

Runtime barrel (src/modules/runtime/index.ts) is all re-export lines; resolveJsonModule is true in tsconfig. Schema $id is "https://mini-stry.local/schema/policy-content.json".

PolicyService already constructs PolicyRepositoryPort + PolicyVersionRepositoryPort. New methods must be added to ports AND every implementing adapter (Convex + memory if one exists for that port).

Existing analog methods to mirror:
- convex-policy-repository.ts: findByRequestType(ctx, requestType) — withIndex("by_tenant_name") read pattern; toPolicyDomain + fromTenantId already imported.
- convex-policy-version-repository.ts: findDraftByPolicy / getNextVersionNumber — withIndex("by_tenant_policy_version") pattern; toPolicyVersionDomain + fromTenantId + fromPolicyId imported.
- convex-approval-task-repository.ts: findByChainId — withIndex(...).collect().map(toApprovalTaskDomain); fromTenantId + fromUserId imported.
- in-memory-approval-task-repository.ts: findByChainId — Map-filter on tenantId + chainId.

Verified Convex indexes (convex/schema.ts): by_tenant_name (policies), by_tenant_policy_version (policyVersions), by_tenant_approver (approvalTasks).

Branded ID factories from barrels: policyId, policyVersionId (policy), userId (directory), approvalTaskId (approval).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add list-by-tenant + list-versions-by-policy read paths to the policy module</name>
  <files>src/modules/policy/ports/policy-repository.port.ts, src/modules/policy/ports/policy-version-repository.port.ts, src/modules/policy/adapters/convex/convex-policy-repository.ts, src/modules/policy/adapters/convex/convex-policy-version-repository.ts, tests/modules/policy/convex-policy-repository.test.ts, tests/modules/policy/convex-policy-version-repository.test.ts</files>
  <read_first>
    - src/modules/policy/ports/policy-repository.port.ts (file being modified; copy the ctx-first signature style from findByRequestType)
    - src/modules/policy/ports/policy-version-repository.port.ts (file being modified; copy findDraftByPolicy signature style)
    - src/modules/policy/adapters/convex/convex-policy-repository.ts (file being modified; mirror findByRequestType withIndex pattern)
    - src/modules/policy/adapters/convex/convex-policy-version-repository.ts (file being modified; mirror getNextVersionNumber / findDraftByPolicy withIndex)
    - .planning/phases/06-admin-portal-ui-dashboard/06-PATTERNS.md (sections "convex-policy-repository.ts — add listByTenant" and "convex-policy-version-repository.ts — add listByPolicy")
    - tests/modules/directory/convex-user-repository.test.ts (analog adapter unit test — fake-db tenant-isolation assertions)
    - tests/_helpers/convex-ctx-fixture.ts (createFakeMutationDb / asQueryDb helpers used by the tests)
    - convex/schema.ts (verify index names by_tenant_name and by_tenant_policy_version)
  </read_first>
  <behavior>
    - Test 1: listByTenant returns only policies whose tenantId matches ctx.tenantId; a policy seeded for tenant B is never returned for tenant A.
    - Test 2: listByPolicy returns only versions matching both ctx.tenantId and the given policyId; versions of another policy (same tenant) are excluded.
    - Test 3: both adapter methods query via the correct withIndex name (by_tenant_name, by_tenant_policy_version) with the tenant equality bound.
  </behavior>
  <action>Before editing the two existing adapter classes, run gitnexus_impact on ConvexPolicyRepository and ConvexPolicyVersionRepository (direction upstream) per CLAUDE.md; warn on HIGH/CRITICAL — these are additive methods so blast radius should be low. Add to PolicyRepositoryPort the method `listByTenant(ctx: TenantContext): Promise<Policy[]>`. Add to PolicyVersionRepositoryPort the method `listByPolicy(ctx: TenantContext, policyId: PolicyId): Promise<PolicyVersion[]>`. Implement listByTenant in convex-policy-repository.ts mirroring findByRequestType: query "policies" withIndex "by_tenant_name" binding q.eq("tenantId", fromTenantId(ctx.tenantId)), then map rows through toPolicyDomain. Implement listByPolicy in convex-policy-version-repository.ts mirroring getNextVersionNumber: query "policyVersions" withIndex "by_tenant_policy_version" binding q.eq("tenantId", fromTenantId(ctx.tenantId)).eq("policyId", fromPolicyId(policyId)), map through toPolicyVersionDomain. Per Pattern 5 prefer order("desc").take(100) over unbounded collect(). Extend tests/modules/policy/ with two adapter unit tests using createFakeMutationDb/asQueryDb (mirror convex-user-repository.test.ts), asserting tenant-A-cannot-see-tenant-B and correct index args.</action>
  <verify>
    <automated>npx vitest run tests/modules/policy/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/modules/policy/` passes including the two new tests.
    - `grep -n "listByTenant" src/modules/policy/ports/policy-repository.port.ts` returns the method signature.
    - `grep -n "listByPolicy" src/modules/policy/ports/policy-version-repository.port.ts` returns the method signature.
    - `grep -n "by_tenant_name" src/modules/policy/adapters/convex/convex-policy-repository.ts` shows the index used by listByTenant.
    - `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>listByTenant and listByPolicy exist on ports + Convex adapters; tenant-isolation tests pass; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add findByApprover read path to the approval module (Convex + memory)</name>
  <files>src/modules/approval/ports/approval-task-repository.port.ts, src/modules/approval/adapters/convex/convex-approval-task-repository.ts, src/modules/approval/adapters/memory/in-memory-approval-task-repository.ts, tests/modules/approval/convex-approval-task-repository.test.ts</files>
  <read_first>
    - src/modules/approval/ports/approval-task-repository.port.ts (file being modified; copy findByChainId signature style)
    - src/modules/approval/adapters/convex/convex-approval-task-repository.ts (file being modified; mirror findByChainId withIndex pattern)
    - src/modules/approval/adapters/memory/in-memory-approval-task-repository.ts (file being modified; mirror findByChainId Map-filter)
    - src/modules/approval/application/approval-routing-service.ts (read the act() unauthorized-approver check at the actorId !== approverId guard to confirm the existing UnauthorizedApproverError behavior the test asserts)
    - .planning/phases/06-admin-portal-ui-dashboard/06-PATTERNS.md (section "convex-approval-task-repository.ts — add findByApprover" and "Memory adapters — mirror each new list method")
    - tests/modules/directory/convex-user-repository.test.ts (analog adapter unit test)
    - tests/_helpers/convex-ctx-fixture.ts (fake-db helpers)
    - convex/schema.ts (verify index by_tenant_approver)
  </read_first>
  <behavior>
    - Test 1: findByApprover returns only tasks where tenantId === ctx.tenantId AND approverId === the given approverId; a task for a different approver (same tenant) is excluded.
    - Test 2: a task in another tenant with the same approverId is excluded (tenant isolation).
    - Test 3: existing act() rejects with UnauthorizedApproverError when ctx.actorId !== task.approverId — assert this case is covered (add a test if not already present in tests/modules/approval/).
  </behavior>
  <action>Run gitnexus_impact on ConvexApprovalTaskRepository (direction upstream) before editing per CLAUDE.md. Add to ApprovalTaskRepositoryPort the method `findByApprover(ctx: TenantContext, approverId: UserId): Promise<ApprovalTask[]>`. Implement it in convex-approval-task-repository.ts mirroring findByChainId: query "approvalTasks" withIndex "by_tenant_approver" binding q.eq("tenantId", fromTenantId(ctx.tenantId)).eq("approverId", fromUserId(approverId)), map through toApprovalTaskDomain (prefer order("desc").take(100)). Implement findByApprover in in-memory-approval-task-repository.ts as a Map-filter on tenantId + approverId (mirror findByChainId). Extend tests/modules/approval/ with the findByApprover scoping tests, and verify (add if missing) the unauthorized-approver act case asserting UnauthorizedApproverError.</action>
  <verify>
    <automated>npx vitest run tests/modules/approval/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/modules/approval/` passes including findByApprover scoping + unauthorized-approver act case.
    - `grep -n "findByApprover" src/modules/approval/ports/approval-task-repository.port.ts` returns the signature.
    - `grep -n "by_tenant_approver" src/modules/approval/adapters/convex/convex-approval-task-repository.ts` shows the index.
    - `grep -n "findByApprover" src/modules/approval/adapters/memory/in-memory-approval-task-repository.ts` shows the memory impl.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>findByApprover on port + Convex adapter + memory fake; scoping + unauthorized-approver tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Export canonical schema from runtime barrel and prove Monaco↔runtime parity</name>
  <files>src/modules/runtime/index.ts, tests/modules/runtime/schema-parity.test.ts</files>
  <read_first>
    - src/modules/runtime/index.ts (file being modified; add one export line mirroring existing re-exports)
    - src/modules/runtime/schema/policy-content.schema.json (the canonical artifact, $id https://mini-stry.local/schema/policy-content.json)
    - src/modules/runtime/adapters/ajv/ajv-schema-validator.ts (read how AjvSchemaValidator loads the schema, to assert object identity in the parity test)
    - tests/modules/runtime/schema-valid.test.ts (analog: valid-policy fixtures to reuse)
    - tests/modules/runtime/schema-invalid.test.ts (analog: invalid-policy fixtures to reuse)
    - .planning/phases/06-admin-portal-ui-dashboard/06-RESEARCH.md (Pattern 4 step 1 + Assumption A1: barrel JSON export with import attribute)
  </read_first>
  <behavior>
    - Test 1 (parity, accept side): for each existing valid fixture, the barrel-exported policyContentSchema validated via Ajv accepts it — and it is the same artifact AjvSchemaValidator already uses.
    - Test 2 (parity, reject side): for each existing invalid fixture, validation against the exported schema rejects it.
    - Test 3 (object identity / single source, D-59): the exported policyContentSchema is the canonical artifact loaded by the runtime validator (assert the $id matches the canonical file's $id and the schema content is identical — not a second editor-specific copy).
  </behavior>
  <action>Run gitnexus_impact on the runtime barrel src/modules/runtime/index.ts (direction upstream) before editing per CLAUDE.md (additive export — low blast radius). Add to src/modules/runtime/index.ts: `export { default as policyContentSchema } from "./schema/policy-content.schema.json" with { type: "json" };`. If the Vite/ESM import-attribute friction surfaces later (Assumption A1), the frontend fallback is a direct relative JSON import — but the barrel export remains the public API per D-59. Create tests/modules/runtime/schema-parity.test.ts modeled on schema-valid.test.ts + schema-invalid.test.ts: import policyContentSchema from the runtime barrel, reuse the existing valid/invalid fixtures, and assert accept⇔accept / reject⇔reject parity plus that the exported schema's $id equals "https://mini-stry.local/schema/policy-content.json" (the canonical single source).</action>
  <verify>
    <automated>npx vitest run tests/modules/runtime/schema-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/modules/runtime/schema-parity.test.ts` passes.
    - `grep -n "policyContentSchema" src/modules/runtime/index.ts` shows the export line.
    - `npm run test:coverage` keeps 100% on src/modules/runtime/**/*.ts.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>policyContentSchema exported from runtime barrel; parity test green; runtime coverage still 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application service → repository adapter | New list methods are the read surface every UI view depends on; tenant scoping is enforced here via indexes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | listByTenant / listByPolicy / findByApprover adapters | mitigate | Every new query uses a by_tenant_* index bound to ctx.tenantId; unit tests assert tenant A never sees tenant B's rows |
| T-06-02 | Elevation of Privilege | findByApprover / approval act | mitigate | findByApprover scopes on approverId; act() retains its actorId !== approverId UnauthorizedApproverError guard (verified, not weakened); test asserts unauthorized case |
| T-06-03 | Tampering | runtime schema export (D-59) | mitigate | Export the single canonical artifact only; parity test forbids a divergent editor-specific schema copy |
</threat_model>

<verification>
- `npm test` green (full suite, dot reporter).
- `npm run test:coverage` meets 90% global / 100% runtime+approval thresholds (new Convex adapter methods are coverage-excluded by config but are still unit-tested via fake-db/memory).
- `npm run lint` clean (import/no-restricted-paths zones intact).
- `npm run typecheck` clean.
</verification>

<success_criteria>
- PolicyRepositoryPort.listByTenant, PolicyVersionRepositoryPort.listByPolicy, ApprovalTaskRepositoryPort.findByApprover exist on ports + all implementing adapters.
- Runtime barrel exports policyContentSchema (D-59).
- schema-parity test + all new tenant-isolation tests pass.
- No domain logic added anywhere; methods are pure indexed reads.
</success_criteria>

<output>
Create `.planning/phases/06-admin-portal-ui-dashboard/06-01-SUMMARY.md` when done.
</output>
