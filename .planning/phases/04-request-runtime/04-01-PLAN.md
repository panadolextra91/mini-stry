---
phase: 04-request-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/request/domain/ids.ts
  - src/modules/request/domain/request-evaluation-status.ts
  - src/modules/request/domain/request-evaluation.ts
  - src/modules/request/domain/request-events.ts
  - src/modules/request/application/errors.ts
  - src/modules/request/application/policy-runtime-service.ts
  - src/modules/request/ports/request-evaluation-repository.port.ts
  - src/modules/request/adapters/convex/mappers.ts
  - src/modules/request/adapters/convex/convex-request-evaluation-repository.ts
  - src/modules/request/adapters/memory/in-memory-request-evaluation-repository.ts
  - src/modules/request/index.ts
  - src/modules/request/README.md
  - src/modules/policy/domain/policy.ts
  - src/modules/policy/application/policy-service.ts
  - src/modules/policy/application/errors.ts
  - src/modules/policy/ports/policy-repository.port.ts
  - src/modules/policy/adapters/convex/convex-policy-repository.ts
  - src/modules/policy/adapters/convex/mappers.ts
  - src/modules/policy/adapters/memory/in-memory-policy-repository.ts
  - src/modules/policy/index.ts
  - convex/schema.ts
  - eslint.config.js
  - tests/_helpers/in-memory-fakes.ts
  - tests/modules/policy/policy-service.test.ts
  - tests/modules/request/policy-runtime-service.test.ts
autonomous: true
requirements: [DEC-01]
must_haves:
  truths:
    - "A caller can submit {requestType, context} for a tenant and receive a RequestEvaluation with status 'completed' carrying the Decision and trace when an active policy resolves and evaluation succeeds"
    - "When no policy exists for the requestType OR the policy has no active version, no RequestEvaluation record is created and a PolicyNotFoundForRequestType / NoActivePolicyError is thrown"
    - "When the evaluator throws EvaluationError (contract violation), a RequestEvaluation with status 'failed', decision null, errorCode and fieldPath set is persisted and the EvaluationError is rethrown"
    - "Policy carries a machine-readable requestType routing key and createPolicy rejects a duplicate [tenantId, requestType]"
    - "A RequestEvaluation belonging to tenant A is never returned to tenant B"
  artifacts:
    - path: "src/modules/request/application/policy-runtime-service.ts"
      provides: "PolicyRuntimeService.submit orchestration (resolve → evaluate → persist three outcome paths)"
      contains: "class PolicyRuntimeService"
    - path: "src/modules/request/domain/request-evaluation.ts"
      provides: "RequestEvaluation entity interface"
      contains: "interface RequestEvaluation"
    - path: "src/modules/request/ports/request-evaluation-repository.port.ts"
      provides: "RequestEvaluationRepositoryPort + CreateRequestEvaluationInput"
      contains: "RequestEvaluationRepositoryPort"
    - path: "src/modules/request/index.ts"
      provides: "request module public barrel"
      exports: ["PolicyRuntimeService", "RequestEvaluation", "RequestEventMap"]
    - path: "convex/schema.ts"
      provides: "requestEvaluations table + policies.requestType field/index"
      contains: "requestEvaluations:"
  key_links:
    - from: "src/modules/request/application/policy-runtime-service.ts"
      to: "src/modules/runtime/index.ts validateAndEvaluate"
      via: "barrel import + call in submit"
      pattern: "validateAndEvaluate"
    - from: "src/modules/request/application/policy-runtime-service.ts"
      to: "src/modules/policy/index.ts PolicyService.getActiveVersion"
      via: "constructor-injected PolicyService"
      pattern: "getActiveVersion"
    - from: "src/modules/request/application/policy-runtime-service.ts"
      to: "src/modules/policy/index.ts PolicyService.findByRequestType"
      via: "submit-time requestType → policyId resolution seam (constructor-injected PolicyService)"
      pattern: "findByRequestType"
    - from: "src/modules/policy/application/policy-service.ts"
      to: "src/modules/policy/ports/policy-repository.port.ts findByRequestType"
      via: "uniqueness check in createPolicy + public findByRequestType passthrough"
      pattern: "findByRequestType"
---

<objective>
Stand up the new `src/modules/request/` module and the `PolicyRuntimeService.submit` orchestration that wraps the pure Phase 2 `validateAndEvaluate` seam with `TenantContext`, policy resolution, and `RequestEvaluation` persistence. Extend the Phase 3 `policy/` module with a machine-readable `requestType` routing key (D-39) and resolution/uniqueness, and extend `convex/schema.ts` with the `requestEvaluations` table (D-38).

Purpose: Deliver DEC-01 — evaluate an `EvaluationContext` against the active policy version for a `requestType` and produce a persisted, structured Decision. Establishes the three outcome paths (success / contract violation / resolution failure) that 04-02 audits.
Output: The `request/` module (entity, branded ID, status union, events, errors, repo port, Convex + in-memory adapters, barrel), `PolicyRuntimeService`, the `policies.requestType` ripple across `policy/`, the Convex schema extension, the ESLint boundary zone, the `setupRequest` test helper, and the `policy-runtime-service.test.ts` outcome-path suite.
</objective>

<execution_context>
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/mini-stry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-request-runtime/04-CONTEXT.md
@.planning/phases/04-request-runtime/04-RESEARCH.md
@.planning/phases/04-request-runtime/04-PATTERNS.md
@.planning/phases/04-request-runtime/04-VALIDATION.md

<interfaces>
<!-- Contracts extracted from the codebase. Use directly — no exploration needed. -->

From src/modules/runtime/index.ts (barrel — import via @/modules/runtime/index.js):

- validateAndEvaluate(validator: SchemaValidatorPort, content: unknown, ctx: EvaluationContext): EvaluationResult
- evaluate, AjvSchemaValidator
- type SchemaValidatorPort, EvaluationContext, Decision, EvaluationResult, TraceEntry, EvaluationErrorCode
- class EvaluationError { code: EvaluationErrorCode; field: string | null; message: string } — NOTE: the field-path property is `field` (NOT `fieldPath`)
- class PolicySchemaInvalidError (NOT a contract violation — must NOT produce a 'failed' record)
- EvaluationResult shape: { decision: Decision; matchedRuleId; evaluationTrace: TraceEntry[] }
- TraceEntry shape: { ruleId: string; matched: boolean }

From src/modules/policy/index.ts (barrel — import via @/modules/policy/index.js):

- class PolicyService { constructor(policyRepo, versionRepo, validator, dispatcher); getActiveVersion(ctx, policyId): Promise<PolicyVersion | null>; createPolicy(ctx, input): Promise<Policy>; findByRequestType(ctx, requestType): Promise<Policy | null> } — findByRequestType is ADDED in this plan (Task 1)
- type Policy { id: PolicyId; tenantId: TenantId; name: string; requestType: string; activeVersionId: PolicyVersionId | null; createdAt: number } — requestType ADDED in this plan; Policy must be exported from the barrel (it is the findByRequestType return type)
- type PolicyId, PolicyVersionId; PolicyVersion has .content (unknown) and .id
- type PolicyRepositoryPort; { EventDispatcher } re-exported
- PolicyVersion.getActiveVersion returns version with .content (unknown — validateAndEvaluate narrows it)

From src/modules/policy/application/policy-service.ts (current — to extend):

- createPolicy(ctx: TenantContext, input: { name: string }): Promise<Policy> { return this.policyRepo.create(ctx, input); }
- getActiveVersion(ctx, policyId): findById then versionRepo.findById(ctx, policy.activeVersionId)

From src/modules/directory/index.ts:

- interface TenantContext { readonly tenantId: TenantId }
- tenantContext(tenantId), tenantId(raw), type TenantId, UserId

From src/shared/event-dispatcher.ts:

- class EventDispatcher<EventMap> { emit(name, payload): Promise<void>; on(name, handler): void } — generic, sequential await

From src/modules/audit/index.ts:

- class AuditEventSubscriber; type AuditLogRepositoryPort, CreateAuditLogInput; InMemoryAuditLogRepository; ConvexAuditLogRepository

From tests/\_helpers/in-memory-fakes.ts:

- setupPolicy(validator): { policyRepo, versionRepo, policyService, dispatcher, auditRepo } — composes EventDispatcher<PolicyEventMap> + void new AuditEventSubscriber(...)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend policy/ with requestType routing key, resolution, and uniqueness (D-39)</name>
  <read_first>
    - src/modules/policy/domain/policy.ts (entity to extend)
    - src/modules/policy/application/policy-service.ts (createPolicy lines 27-32, getActiveVersion lines 223-232)
    - src/modules/policy/application/errors.ts (DraftAlreadyExistsError lines 57-64 — error template)
    - src/modules/policy/ports/policy-repository.port.ts (create input + port shape)
    - src/modules/policy/adapters/convex/convex-policy-repository.ts (insert + withIndex query pattern)
    - src/modules/policy/adapters/convex/mappers.ts (toPolicyDomain lines 18-24)
    - src/modules/policy/adapters/memory/in-memory-policy-repository.ts (create + tenant-scoped find)
    - src/modules/policy/index.ts (barrel exports to extend)
    - tests/modules/policy/policy-service.test.ts (createPolicy call sites ~lines 35-50 that break on signature change)
    - .planning/phases/04-request-runtime/04-PATTERNS.md §"Modified-File Impact" (the d=1 ripple table)
    - .planning/phases/04-request-runtime/04-RESEARCH.md §Pitfall 1, Pitfall 5
  </read_first>
  <action>
    MANDATORY FIRST (CLAUDE.md): run `gitnexus_impact({target: "createPolicy", direction: "upstream"})` and `gitnexus_impact({target: "Policy", direction: "upstream"})` via the GitNexus MCP tools and report the blast radius (direct callers, risk level). If HIGH/CRITICAL, surface to the user before editing. This `policies.requestType` change touches Phase-3-owned code across nine d=1 dependents — update them together so TypeScript stays green.

    Add `readonly requestType: string` to the `Policy` interface in policy.ts (per D-39 — stable machine-readable routing key, distinct from display `name`).

    In policy-repository.port.ts: change `create` input type to `{ name: string; requestType: string }`, and add `findByRequestType(ctx: TenantContext, requestType: string): Promise<Policy | null>`.

    In convex-policy-repository.ts: insert `requestType` on `create`; implement `findByRequestType` querying the `by_tenant_request_type` index with `.unique()` (safe — uniqueness enforced at create time). In policy mappers.ts add `requestType: doc.requestType` inside `toPolicyDomain`. In in-memory-policy-repository.ts: store `requestType` on create and implement `findByRequestType` filtering by tenant + requestType.

    In policy-service.ts: change `createPolicy` signature to `createPolicy(ctx: TenantContext, input: { name: string; requestType: string }): Promise<Policy>`. Inside, BEFORE create, call `this.policyRepo.findByRequestType(ctx, input.requestType)`; if non-null, throw `RequestTypeAlreadyExistsError(input.requestType)` (application-layer uniqueness per Pitfall 5 — Convex indexes do NOT enforce uniqueness, mirrors the `[tenantId, name]` role pattern).

    Also in policy-service.ts add the PUBLIC resolution method `findByRequestType(ctx: TenantContext, requestType: string): Promise<Policy | null>` (name it `findByRequestType`, consistent with the repo port). It takes `ctx` FIRST (TenantContext-first, D-19) and delegates straight to the port: it returns `this.policyRepo.findByRequestType(ctx, requestType)`. This is the concrete public seam that `PolicyRuntimeService.submit` calls at RESOLVE time (Task 3) — resolution is owned by the policy module per RESEARCH Pitfall 5 / A3, never duplicated in `request/`. Keep it a thin `ctx`-first passthrough: no ambient resolution, no business logic beyond the tenant scoping the repo already applies.

    In policy errors.ts add `RequestTypeAlreadyExistsError extends Error` (mirror `DraftAlreadyExistsError`: public readonly ctor field, `this.name` set). Export it from policy/index.ts together with the new port type changes. Ensure the policy barrel (`src/modules/policy/index.ts`) surfaces the `Policy` type (it is the `findByRequestType` return type consumed by `PolicyRuntimeService` in Task 3) — `Policy` is already listed in the `<interfaces>` block; if the barrel does not re-export it, add the export.

    In convex/schema.ts: add `requestType: v.string()` to the `policies` table and add `.index("by_tenant_request_type", ["tenantId", "requestType"])` (tenant-prefixed per D-09), keeping the existing `by_tenant_name` index.

    In tests/modules/policy/policy-service.test.ts: update every `createPolicy(TENANT, { name })` call site to include `requestType`, and add a `requestType` test block asserting (a) requestType persists on the returned Policy, (b) a second `createPolicy` with the same `[tenantId, requestType]` throws `RequestTypeAlreadyExistsError`, (c) the same requestType under a DIFFERENT tenant succeeds, (d) `findByRequestType(ctx, requestType)` returns the matching tenant-scoped Policy and `null` for an unknown requestType.

    Do NOT make requestType nullable; do NOT add a DB-level unique constraint; do NOT touch the evaluator or runtime contract.

  </action>
  <behavior>
    - createPolicy persists requestType and returns it on the Policy
    - createPolicy with a duplicate [tenantId, requestType] throws RequestTypeAlreadyExistsError
    - the same requestType succeeds under a different tenant (tenant isolation, CON-01)
    - findByRequestType (repo + PolicyService passthrough) returns the matching tenant-scoped Policy or null
  </behavior>
  <verify>
    <automated>npx vitest run tests/modules/policy/policy-service.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/policy/domain/policy.ts` contains `readonly requestType: string`
    - `src/modules/policy/application/policy-service.ts` defines a public `findByRequestType(ctx: TenantContext, requestType: string): Promise<Policy | null>` that delegates to `this.policyRepo.findByRequestType(ctx, requestType)` with `ctx` first (D-19)
    - `src/modules/policy/index.ts` exports `RequestTypeAlreadyExistsError` and the `Policy` type (the findByRequestType return type)
    - `src/modules/policy/application/errors.ts` defines `RequestTypeAlreadyExistsError`
    - `convex/schema.ts` contains `by_tenant_request_type` on the `policies` table and `requestType: v.string()`
    - `npx vitest run tests/modules/policy/policy-service.test.ts --reporter=dot` exits 0 with a passing `requestType` uniqueness test and a `findByRequestType` resolution test
    - `gitnexus_impact` was run on `createPolicy` and `Policy` and the blast radius reported
  </acceptance_criteria>
  <done>requestType routing key threaded through Policy entity, port, both adapters, mapper, service (with uniqueness AND a public findByRequestType resolution method), schema, and tests — full suite for the policy module green.</done>
</task>

<task type="auto">
  <name>Task 2: Create the request/ module (entity, ID, status, events, errors, port, adapters, barrel) + schema table + ESLint zone</name>
  <read_first>
    - src/modules/policy/domain/ids.ts (branded-ID template)
    - src/modules/policy/domain/policy.ts (entity interface shape)
    - src/modules/policy/domain/policy-version-status.ts (status union template)
    - src/modules/policy/domain/policy-events.ts (event map template lines 1-40)
    - src/modules/policy/application/errors.ts (error class template)
    - src/modules/audit/ports/audit-log-repository.port.ts (CreateXInput + port template)
    - src/modules/audit/adapters/memory/in-memory-audit-log-repository.ts (in-memory fake template)
    - src/modules/policy/adapters/convex/convex-policy-repository.ts (Convex repo template)
    - src/modules/policy/adapters/convex/mappers.ts (mapper template + toPolicyVersionId/fromPolicyVersionId)
    - src/modules/directory/adapters/convex/mappers.ts (toTenantId/fromTenantId)
    - src/modules/policy/index.ts (barrel grouping template)
    - convex/schema.ts (policyVersions/policies table convention)
    - eslint.config.js (lines 28-47 module-boundary zones)
    - .planning/phases/04-request-runtime/04-PATTERNS.md (exact per-file analog assignments)
    - .planning/phases/04-request-runtime/04-RESEARCH.md §Pattern 2, §Pattern 4
  </read_first>
  <action>
    Create `src/modules/request/` mirroring the `policy/` module shape exactly (D-08 barrel-only cross-module surface).

    domain/ids.ts: branded `RequestEvaluationId = string & { readonly __brand: "RequestEvaluationId" }` + factory `requestEvaluationId(raw)`.
    domain/request-evaluation-status.ts: `export type RequestEvaluationStatus = "completed" | "failed"`.
    domain/request-evaluation.ts: `interface RequestEvaluation` with readonly fields `id: RequestEvaluationId`, `tenantId: TenantId`, `requestType: string`, `requestInput: EvaluationContext`, `policyVersionId: PolicyVersionId` (non-null per D-41), `decision: Decision | null` (null on failure per D-40), `trace: readonly TraceEntry[]` ({ruleId,matched} only per D-42), `status: RequestEvaluationStatus`, `errorCode: EvaluationErrorCode | null`, `fieldPath: string | null`, `createdAt: number`. Import `Decision`/`TraceEntry`/`EvaluationContext`/`EvaluationErrorCode` from the runtime barrel, `PolicyVersionId` from the policy barrel, `TenantId` from the directory barrel.
    domain/request-events.ts: `RequestEvaluatedEvent` { tenantId, evaluationRecordId, timestamp }, `EvaluationFailedEvent` { tenantId, evaluationRecordId, errorCode, timestamp }, `ResolutionFailedEvent` { tenantId, requestType, reason, timestamp } (NO evaluationRecordId — none exists per D-41), and `RequestEventMap = { RequestEvaluated; EvaluationFailed; ResolutionFailed }`.
    application/errors.ts: `PolicyNotFoundForRequestType` and `NoActivePolicyError` (each extends Error, public readonly requestType ctor field, this.name set).
    ports/request-evaluation-repository.port.ts: `CreateRequestEvaluationInput` (omits id/tenantId/createdAt — adapter assigns them, mirroring CreateAuditLogInput) with requestType, requestInput, policyVersionId, decision, trace, status, errorCode, fieldPath; and `RequestEvaluationRepositoryPort` with `create(ctx, input)`, `findById(ctx, id)`, `findByTenant(ctx)` (ctx first on every method, D-19).
    adapters/memory/in-memory-request-evaluation-repository.ts: Map keyed by id + idCounter; `create` stamps `tenantId: ctx.tenantId` and `createdAt: Date.now()`; `findById` returns null when `r.tenantId !== ctx.tenantId` (tenant scoping, CON-01); `findByTenant` filters by tenant.
    adapters/convex/mappers.ts: `toRequestEvaluationId`/`fromRequestEvaluationId` + `toRequestEvaluationDomain(doc)` casting `decision`/`trace`/`requestInput` from the v.any()-shaped doc (same pattern as policyVersions.content); reuse `toTenantId` (directory mappers) and `toPolicyVersionId` (policy mappers) via the established adapter→adapter deep-import convention.
    adapters/convex/convex-request-evaluation-repository.ts: db-handle ctor; `create` inserts into `requestEvaluations` (using fromTenantId/fromPolicyVersionId), gets the doc, returns `toRequestEvaluationDomain`; `findById` (get + tenant check); `findByTenant` (.withIndex("by_tenant_created", q => q.eq("tenantId", ...)).collect()).
    index.ts: barrel exporting RequestEvaluation, RequestEvaluationId + requestEvaluationId, RequestEvaluationStatus, RequestEventMap + the three event types, RequestEvaluationRepositoryPort + CreateRequestEvaluationInput, PolicyNotFoundForRequestType + NoActivePolicyError, ConvexRequestEvaluationRepository, the mapper fns, InMemoryRequestEvaluationRepository, and re-export `EventDispatcher` from `@/shared/event-dispatcher.js` (mirror policy index). PolicyRuntimeService is added to this barrel in Task 3.
    README.md: short module README mirroring policy/README.md.

    In convex/schema.ts add the `requestEvaluations` table per RESEARCH Pattern 4: tenantId (v.id("tenants")), requestType (v.string()), requestInput (v.any()), policyVersionId (v.id("policyVersions")), decision (v.union(v.any(), v.null())), trace (v.array(v.object({ ruleId: v.string(), matched: v.boolean() }))), status (v.string()), errorCode (v.union(v.string(), v.null())), fieldPath (v.union(v.string(), v.null())), createdAt (v.number()); indexes `by_tenant_created` ["tenantId","createdAt"] and `by_tenant_request_type` ["tenantId","requestType"].

    In eslint.config.js add a `request` `import/no-restricted-paths` zone mirroring the four existing ones: target `./src/modules/!(request)/**/*`, from `./src/modules/request/{domain,application,adapters}/**/*`, message referencing D-08.

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx eslint src/modules/request --max-warnings=0</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/request/index.ts` exports `RequestEvaluation`, `RequestEventMap`, `RequestEvaluationRepositoryPort`, `InMemoryRequestEvaluationRepository`, `ConvexRequestEvaluationRepository`
    - `src/modules/request/domain/request-evaluation.ts` contains `interface RequestEvaluation` with `decision: Decision | null` and `policyVersionId: PolicyVersionId` (non-null)
    - `convex/schema.ts` contains `requestEvaluations:` with `by_tenant_request_type` index
    - `eslint.config.js` contains a zone with `from: "./src/modules/request/{domain,application,adapters}/**/*"`
    - `npx tsc --noEmit` reports no errors in `src/modules/request`; `npx eslint src/modules/request` passes
  </acceptance_criteria>
  <done>The request/ module skeleton compiles and lints clean, the requestEvaluations table is in the schema, and the boundary zone is registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PolicyRuntimeService.submit — three outcome paths + setupRequest helper + service test suite (DEC-01)</name>
  <read_first>
    - src/modules/policy/application/policy-service.ts (ctor-DI lines 19-25, getActiveVersion lines 223-232, the new public findByRequestType from Task 1, emit shape lines 69-77)
    - src/modules/runtime/application/policy-runtime.ts (validateAndEvaluate seam; header names Phase 4)
    - src/modules/runtime/application/errors.ts (EvaluationError: code + field; PolicySchemaInvalidError)
    - src/modules/request/index.ts (the barrel created in Task 2)
    - src/modules/request/ports/request-evaluation-repository.port.ts (CreateRequestEvaluationInput)
    - tests/_helpers/in-memory-fakes.ts (setupPolicy lines 22-37 — the helper template)
    - tests/modules/policy/policy-service.test.ts (test-suite style + how a published active version is set up)
    - .planning/phases/04-request-runtime/04-PATTERNS.md §policy-runtime-service.ts (authoritative skeleton + Pitfall 3 note)
    - .planning/phases/04-request-runtime/04-RESEARCH.md §Pattern 1 (lines 217-291), §Open Question 1
    - .planning/phases/04-request-runtime/04-VALIDATION.md (Per-Task Verification Map: success / resolution / contract / tenant)
  </read_first>
  <action>
    Create `src/modules/request/application/policy-runtime-service.ts` with `class PolicyRuntimeService`. Constructor (all private readonly, no ambient resolution per D-18): `policyService: PolicyService`, `validator: SchemaValidatorPort`, `evalRepo: RequestEvaluationRepositoryPort`, `dispatcher: EventDispatcher<RequestEventMap>`.

    `async submit(ctx: TenantContext, input: { requestType: string; context: EvaluationContext }): Promise<RequestEvaluation>` (ctx FIRST, D-19) with three outcome paths:

    (1) RESOLVE (precondition, D-41): resolve `requestType → policyId` by calling `const policy = await this.policyService.findByRequestType(ctx, input.requestType)` — the public resolution method added to `PolicyService` in Task 1 (the policy module owns resolution per the locked design decision / RESEARCH Pitfall 5; do NOT add a separate resolution port in `request/`). If `policy` is null (D-41 no-record path) → `await this.dispatcher.emit("ResolutionFailed", { tenantId: ctx.tenantId, requestType: input.requestType, reason: "POLICY_NOT_FOUND", timestamp: Date.now() })` then `throw new PolicyNotFoundForRequestType(input.requestType)`. Otherwise `const version = await this.policyService.getActiveVersion(ctx, policy.id)`; if null → emit `ResolutionFailed` with reason `"NO_ACTIVE_VERSION"` then `throw new NoActivePolicyError(input.requestType)`. CRITICAL: NO RequestEvaluation record is created on either resolution failure (D-41).

    (2) EVALUATE: `const result = validateAndEvaluate(this.validator, version.content, input.context)` (default to re-validation for RUN-03 defense-in-depth per CONTEXT discretion) inside a try block.

    (3a) SUCCESS: `const record = await this.evalRepo.create(ctx, { requestType: input.requestType, requestInput: input.context, policyVersionId: version.id, decision: result.decision, trace: result.evaluationTrace, status: "completed", errorCode: null, fieldPath: null })`; then `await this.dispatcher.emit("RequestEvaluated", { tenantId: ctx.tenantId, evaluationRecordId: record.id, timestamp: Date.now() })`; return record.

    (3b) CONTRACT VIOLATION (catch): `if (err instanceof EvaluationError)` → `const record = await this.evalRepo.create(ctx, { requestType: input.requestType, requestInput: input.context, policyVersionId: version.id, decision: null, trace: [], status: "failed", errorCode: err.code, fieldPath: err.field })`; then `await this.dispatcher.emit("EvaluationFailed", { tenantId: ctx.tenantId, evaluationRecordId: record.id, errorCode: err.code, timestamp: Date.now() })`; then `throw err` (D-40 rethrow). For any non-EvaluationError (e.g. PolicySchemaInvalidError) → `throw err` with NO 'failed' record (D-40 anti-pattern guard).

    PITFALL 3 / OPEN QUESTION 1 — DECISION FOR v1 (state in SUMMARY): persist `trace: []` on the failed record. The pure evaluator builds trace locally and discards it on throw; EvaluationError carries only `code` + `field`, not partial trace. Honoring "no evaluator-contract change" (D-42), the failed record stores empty trace + errorCode + fieldPath; replay reconstructs up to the failing rule. A non-empty partial trace is explicitly out of scope for v1.

    Note `EvaluationError.field` maps to the entity's `fieldPath` column (the runtime property is `field`).

    Add `PolicyRuntimeService` to `src/modules/request/index.ts`.

    Add `setupRequest(validator: SchemaValidatorPort)` to tests/_helpers/in-memory-fakes.ts mirroring `setupPolicy`: compose `setupPolicy(validator)` to get a wired `policyService`, build a `new EventDispatcher<RequestEventMap>()`, an `InMemoryRequestEvaluationRepository`, `void new RequestAuditSubscriber(auditRepo, dispatcher)` (the subscriber class is created in 04-02 — for THIS plan, wire the dispatcher + repo + service and leave the subscriber wiring as a clearly marked TODO comment to be completed in 04-02; do NOT block the service tests on it), and `new PolicyRuntimeService(policyService, validator, evalRepo, dispatcher)`. Return `{ service, evalRepo, dispatcher, policyService, auditRepo }`.

    Create `tests/modules/request/policy-runtime-service.test.ts` with three describe/it blocks named so the VALIDATION map test filters match — `-t "success"`, `-t "resolution"`, `-t "contract"` — plus a `-t "tenant"` isolation test:
    - success: seed a policy (createPolicy with requestType) + published active version with content that matches the input → submit → assert returned record status 'completed', decision equals the evaluator's decision, trace deep-equals result.evaluationTrace, and a record exists in evalRepo.
    - resolution: (a) submit an unknown requestType → expect PolicyNotFoundForRequestType thrown (findByRequestType returned null) AND evalRepo.findByTenant empty (no record); (b) a policy with no active version → expect NoActivePolicyError AND no record.
    - contract violation: seed an active version whose evaluation throws EvaluationError (e.g. MISSING_FIELD via an input missing a required field) → submit → expect the EvaluationError rethrown AND a persisted record with status 'failed', decision null, errorCode set, fieldPath set, trace [].
    - tenant: a record created under TENANT_A is not returned by evalRepo.findById under TENANT_B.

  </action>
  <behavior>
    - submit success → RequestEvaluation{status:'completed', decision, trace} returned and persisted; RequestEvaluated emitted
    - submit with unknown requestType (findByRequestType → null) or no active version → throws, NO record persisted, ResolutionFailed emitted
    - submit with EvaluationError → RequestEvaluation{status:'failed', decision:null, errorCode, fieldPath, trace:[]} persisted, EvaluationFailed emitted, error rethrown
    - non-EvaluationError (PolicySchemaInvalidError) → rethrown with no 'failed' record
    - tenant A's record is invisible to tenant B
  </behavior>
  <verify>
    <automated>npx vitest run tests/modules/request/policy-runtime-service.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/request/application/policy-runtime-service.ts` contains `class PolicyRuntimeService` with `submit(ctx`
    - `submit` resolves via `this.policyService.findByRequestType(ctx, input.requestType)` and treats a null return as the D-41 no-record resolution failure
    - `src/modules/request/index.ts` exports `PolicyRuntimeService`
    - `tests/_helpers/in-memory-fakes.ts` defines `setupRequest`
    - `npx vitest run tests/modules/request/policy-runtime-service.test.ts -t "success" --reporter=dot` exits 0
    - `npx vitest run tests/modules/request/policy-runtime-service.test.ts -t "resolution" --reporter=dot` exits 0
    - `npx vitest run tests/modules/request/policy-runtime-service.test.ts -t "contract" --reporter=dot` exits 0
    - `npx vitest run tests/modules/request/policy-runtime-service.test.ts -t "tenant" --reporter=dot` exits 0
    - SUMMARY records the v1 partial-trace decision (empty trace + errorCode/fieldPath)
  </acceptance_criteria>
  <done>PolicyRuntimeService.submit implements all three outcome paths with no record on resolution failure (via PolicyService.findByRequestType returning null), a failed record + rethrow on contract violation, and a completed record on success; the four-scenario test suite is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                              |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| caller → PolicyRuntimeService.submit      | untrusted `{requestType, context}` crosses into orchestration; tenant identity arrives via TenantContext |
| service → RequestEvaluationRepositoryPort | persistence of tenant-scoped operational records                                                         |
| service → runtime (validateAndEvaluate)   | untrusted EvaluationContext + stored policy content cross into the pure evaluator                        |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                            |
| --------- | ---------------------- | ------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-04-01   | Information Disclosure | RequestEvaluation reads/writes (repo)                              | mitigate    | Every repo method takes `ctx: TenantContext` first and scopes by `ctx.tenantId`; `findById` returns null on tenant mismatch; tenant-prefixed indexes (D-09). Verified by the `-t "tenant"` isolation test (CON-01).                                                                        |
| T-04-02   | Tampering              | submit() evaluation path                                           | mitigate    | A malformed request that triggers `EvaluationError` (D-40) is NEVER coerced into a Decision — it produces `status:'failed'`, `decision:null`, and the error is rethrown. Asserted by the `-t "contract"` test.                                                                             |
| T-04-03   | Tampering              | validateAndEvaluate input                                          | mitigate    | `validateAndEvaluate` runs Ajv schema validation (RUN-03) before `evaluate()`; a schema-invalid version throws `PolicySchemaInvalidError` and is NOT recorded as a business 'failed' record (anti-pattern guard). No `eval()` / dynamic code anywhere (Security Constraint).               |
| T-04-04   | Spoofing/Tampering     | createPolicy requestType uniqueness + findByRequestType resolution | mitigate    | Application-layer `[tenantId, requestType]` uniqueness in `createPolicy` (Convex indexes do not enforce uniqueness); a duplicate throws `RequestTypeAlreadyExistsError`, so `PolicyService.findByRequestType` resolves at most one policy at submit time, preventing ambiguous resolution. |
| T-04-SC   | Tampering              | npm/pip/cargo installs                                             | accept      | Phase 4 installs ZERO packages (RESEARCH Package Legitimacy Audit: not applicable). No registry fetch occurs; `package.json` unchanged. If an install is attempted, treat as a design error and stop.                                                                                      |

</threat_model>

<verification>
- `npx vitest run tests/modules/policy/policy-service.test.ts --reporter=dot` — policy ripple green (requestType + uniqueness + findByRequestType resolution)
- `npx vitest run tests/modules/request --reporter=dot` — request service outcome paths green
- `npx tsc --noEmit` — no type errors after the requestType ripple
- `npx eslint src/modules/request --max-warnings=0` — boundary zone clean
- `npm test` — full suite green (createPolicy ripple verified across policy + audit suites)
- Manual: `npm run convex:dev` registers the `requestEvaluations` table + `policies.by_tenant_request_type` index without validation errors
</verification>

<success_criteria>

- PolicyRuntimeService.submit runs a submitted EvaluationContext against the active policy version for a requestType and returns a structured Decision (DEC-01, ROADMAP success criterion 1)
- requestType → policyId resolution flows through the concrete public `PolicyService.findByRequestType` seam; a null return is the D-41 no-record resolution failure
- Three outcome paths behave per D-40/D-41: success persists 'completed', contract violation persists 'failed' + rethrows, resolution failure persists no record + throws
- Policy carries an immutable machine-readable requestType with application-layer [tenantId, requestType] uniqueness (D-39)
- requestEvaluations table + policies.requestType field/index exist in convex/schema.ts (D-38)
- request/ module boundary enforced by an ESLint zone (D-08); zero new packages added
  </success_criteria>

<output>
Create `.planning/phases/04-request-runtime/04-01-SUMMARY.md` when done. Record the v1 partial-trace decision (empty trace + errorCode/fieldPath) and the GitNexus impact result for createPolicy/Policy.
</output>
</content>
</invoke>
