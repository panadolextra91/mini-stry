---
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/modules/policy/domain/entities.ts
  - src/modules/policy/domain/types.ts
requirements:
  - POL-05
  - POL-06
  - AUD-03
autonomous: true
---

# Plan 01-01: Bootstrap TypeScript/Vitest Environment and Foundational Domain Models

## Objective

Set up the modular monolith Hexagonal folder layout in `src/modules/policy`, configure the Vitest environment for unit testing, and implement the core domain models (Policy, PolicyVersion, AuditLog, Tenant, User, Role skeletons) in pure TypeScript.

## Verification Criteria

1. Modular directories under `src/modules/policy/` exist and match Hexagonal Architecture guidelines.
2. `vitest.config.ts` is configured and passes validation.
3. TypeScript compiler compiles `entities.ts` and `types.ts` successfully without error.

## must_haves

1. TypeScript compiler and Vitest runner must execute successfully.
2. `entities.ts` must export `PolicyEntity`, `PolicyVersionEntity`, and `AuditLogEntity` skeletons.
3. Domain files must contain 0 direct dependencies or imports from external database frameworks.

## Tasks

<task>
<action>
Configure modular directories and Vitest testing stack.
Update `package.json` with vitest devDependencies and test execution script.
Create `vitest.config.ts` path configuration file.
</action>
<read_first>
- package.json
</read_first>
<acceptance_criteria>
- package.json has a "test" script calling "vitest run"
- package.json contains DevDependencies for "vitest"
- vitest.config.ts compiles successfully and registers path mappings
- Directories exist:
  - src/modules/policy/domain
  - src/modules/policy/ports
  - src/modules/policy/application
</acceptance_criteria>
</task>

<task>
<action>
Create core domain AST and Token types in `src/modules/policy/domain/types.ts`.
Create core domain entity classes `PolicyEntity`, `PolicyVersionEntity`, `AuditLogEntity`, `TenantEntity`, `UserEntity`, and `RoleEntity` in `src/modules/policy/domain/entities.ts`.
Keep all domain code pure TypeScript, zero imports from any database ORM or Convex server libraries.
</action>
<read_first>
- src/modules/policy/domain/types.ts
- src/modules/policy/domain/entities.ts
</read_first>
<acceptance_criteria>
- types.ts defines compiler interfaces: Token, TokenType, and ASTNode
- entities.ts exports `PolicyEntity` (id, tenantId, name)
- entities.ts exports `PolicyVersionEntity` (id, policyId, version, content, status)
- entities.ts exports `AuditLogEntity` (id, tenantId, actorId, action, targetType, targetId, details, timestamp)
- entities.ts exports `TenantEntity` (id, name), `RoleEntity` (id, tenantId, name), and `UserEntity` (id, tenantId, name, email, roleId, managerId)
- All entity fields are strongly typed, and no platform-specific database imports exist in these files
</acceptance_criteria>
</task>
